<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>ASR-CMVN</title>
    <url>/2021/08/24/ASR-CMVN/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>&nbsp;&nbsp;&nbsp;&nbsp;CMVN在语音识别中的作用</p>
<span id="more"></span>
<h2 id="noun-翻译"><a href="#noun-翻译" class="headerlink" title="noun 翻译"></a>noun 翻译</h2><p>&nbsp;&nbsp;&nbsp;&nbsp;CMN：Cepstral Mean Normalisation，倒谱均值归一化；<br>&nbsp;&nbsp;&nbsp;&nbsp;CMVN：Cepstral Mean Variance Normalisation，倒谱均值方差归一化；</p>
<h2 id="CMVN"><a href="#CMVN" class="headerlink" title="CMVN"></a>CMVN</h2><p>&nbsp;&nbsp;&nbsp;&nbsp;CMVN与数据归一化相似，但CMVN的意义更偏向于对音频数据进行滤波，可提升识别精度。<br>&nbsp;&nbsp;&nbsp;&nbsp;在线的CMVN是基于滑动窗口进行的，会影响语音识别响应时间；feature norm是对所有训练数据进行统计，然后单帧计算<br>&nbsp;&nbsp;&nbsp;&nbsp;因CMVN占用识别响应时间，最新的端到端识别中未使用<br>&nbsp;&nbsp;&nbsp;&nbsp;CMVN详细原理可参考References中的论文[1]</p>
<h3 id="References"><a href="#References" class="headerlink" title="References"></a>References</h3><p>[1]<a href="http://www.apsipa.org/proceedings/2020/pdfs/0000532.pdf">《Significance of CMVN for Replay Spoof Detection》</a></p>
]]></content>
      <tags>
        <tag>ASR</tag>
        <tag>Feature-engineering</tag>
      </tags>
  </entry>
  <entry>
    <title>Adaptive RAG：从相似度检索到信息需求调度</title>
    <url>/2026/06/10/AI-Adaptive-RAG-Retrieval-Scheduling/</url>
    <content><![CDATA[<p>RAG 的第一阶段很直观：把文档切块，向量化，按相似度取回若干段，再交给 LLM 生成答案。这个流程足够实用，但它默认了一个强假设：模型在生成前就知道自己需要什么信息。复杂问题里，这个假设经常不成立。</p>
<p>更值得关注的方向是 Adaptive RAG。它不是单纯换一个向量库，而是把检索变成生成过程中的调度动作：什么时候检索、检索什么、检索几次、是否改写查询、是否把证据图谱化，都由任务状态决定。</p>
<span id="more"></span>

<h2 id="固定检索的问题"><a href="#固定检索的问题" class="headerlink" title="固定检索的问题"></a>固定检索的问题</h2><p>固定检索通常有三个隐性成本。</p>
<p>第一，召回时机固定。很多问题只有生成到一半才暴露信息缺口，提前检索会把上下文预算花在弱相关材料上。</p>
<p>第二，检索粒度固定。一个事实核验问题可能只需要一个段落；一个归因问题可能需要实体、关系和多跳路径。统一取 top-k chunk 会把不同类型的信息需求压成同一个接口。</p>
<p>第三，失败原因不清晰。答案错了，可能是查询写得差、召回不足、证据冲突、排序失败，也可能是生成阶段没有忠实使用证据。如果系统没有把这些阶段拆开记录，后续只能凭感觉调参数。</p>
<h2 id="四类触发机制"><a href="#四类触发机制" class="headerlink" title="四类触发机制"></a>四类触发机制</h2><p>Adaptive RAG 可以按触发机制拆开看。</p>
<p><strong>显式 token 触发</strong>：Self-RAG 这类方法让模型生成控制标记，决定是否检索、是否继续、答案是否被支撑。优点是可解释性强，缺点是需要训练或 prompt 约束来保证控制标记稳定。</p>
<p><strong>不确定性触发</strong>：当模型分布熵升高、候选答案分歧变大，或关键槽位没有依据时再检索。这类方法适合做轻量工程化，但要防止把语言模型的自信误当成事实可靠性。</p>
<p><strong>信息需求触发</strong>：DRAGIN 这类思路更强调“当前生成需要什么信息”。它关心的不是泛泛的不确定，而是上下文里缺少哪类证据。</p>
<p><strong>工作流触发</strong>：Agentic RAG 把检索作为工作流节点，与查询改写、工具调用、证据压缩、反思和答案校验组合。它的上限更高，但状态管理和日志要求也更高。</p>
<h2 id="GraphRAG-的位置"><a href="#GraphRAG-的位置" class="headerlink" title="GraphRAG 的位置"></a>GraphRAG 的位置</h2><p>GraphRAG 和 KG-RAG 解决的是另一个维度：检索对象不再只是文本块，而是实体、关系、社区摘要和路径。它特别适合需要跨文档归纳的问题，比如“某个技术路线为什么演化成现在这样”。</p>
<p>不过图结构不是免费午餐。构图质量、实体消歧、关系抽取、更新频率、图检索召回都会影响最终效果。工程上可以先把图当成证据组织层，而不是一开始就替换所有向量检索。</p>
<h2 id="开源落地接口"><a href="#开源落地接口" class="headerlink" title="开源落地接口"></a>开源落地接口</h2><p>一个可维护的 Adaptive RAG 系统，建议至少把接口拆成五层：</p>
<ol>
<li>query planner：决定原问题是否需要拆解；</li>
<li>retriever router：选择向量检索、关键词检索、图检索或混合检索；</li>
<li>evidence store：保留证据来源、版本、分数和路径；</li>
<li>context composer：控制证据压缩、排序和上下文预算；</li>
<li>answer verifier：检查答案是否被证据支撑。</li>
</ol>
<p>这样做的价值是每一层都能单独评测。召回不够就看 retriever，证据过长就看 composer，答案跑偏就看 verifier，而不是把所有错误都归因给 LLM。</p>
<h2 id="评测不要只看最终答案"><a href="#评测不要只看最终答案" class="headerlink" title="评测不要只看最终答案"></a>评测不要只看最终答案</h2><p>Adaptive RAG 的评测应该拆成阶段指标：</p>
<ul>
<li>查询拆解是否覆盖必要子问题；</li>
<li>召回证据是否包含关键事实；</li>
<li>证据排序是否把关键材料放进上下文窗口；</li>
<li>生成答案是否引用了正确证据；</li>
<li>无证据时是否拒答或提示不确定。</li>
</ul>
<p>如果只看最终答案准确率，很难知道系统是因为检索做对了，还是模型靠参数记忆猜对了。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>Adaptive RAG 的核心不是“多检索几次”，而是让检索成为可观测、可调度、可评测的动作。真正的工程分界线也不在向量库品牌，而在系统是否能回答：这次检索为什么发生，拿到了什么证据，证据如何进入上下文，答案为什么可信。</p>
<h2 id="Retrieval-decision-log"><a href="#Retrieval-decision-log" class="headerlink" title="Retrieval decision log"></a>Retrieval decision log</h2><p>Adaptive RAG should keep a decision log for every answer. A useful record contains the trigger reason, rewritten query, retriever type, top evidence, discarded evidence, final context window, and fallback path.</p>
<p>This log turns retrieval from a hidden prompt side effect into an auditable policy. When an answer is wrong, the error can be assigned to trigger timing, query decomposition, recall, reranking, context compression, or generation not following evidence. That split is more actionable than only judging the final answer.</p>
]]></content>
      <tags>
        <tag>AI</tag>
        <tag>RAG</tag>
        <tag>LLM</tag>
        <tag>Open Source</tag>
      </tags>
  </entry>
  <entry>
    <title>ASR-CTC</title>
    <url>/2021/08/29/ASR-CTC/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>&nbsp;&nbsp;&nbsp;&nbsp;CTC(Connectionist Temporal Classification)可以在未对输入序列与输出标签进行FA(Force Alignment)的情况下完成时序对齐及分类工作，主要用于语音识别及手写识别[1]。</p>
<span id="more"></span>
<h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>&nbsp;&nbsp;&nbsp;&nbsp;CTC的工作原理如下图所示[1]:</p>
<p><img src="/2021/08/29/ASR-CTC/CTC.png"></p>
<p>&nbsp;&nbsp;&nbsp;&nbsp;以ASR为例，给定语音信号输入序列$X&#x3D;[x_1, x_2,…, x_T]$，输出序列记为$Y&#x3D;[y_1, y_2, …, y_U]$，当前需要找到$X$到$Y$的精确映射。<br>&nbsp;&nbsp;&nbsp;&nbsp;因以下几点的存在使得寻找映射的工作比较困难：<br>1)X和Y是可变的序列<br>2)X和Y的长度比率不是固定的<br>3)X和Y的对应关系事先无法确定<br>&nbsp;&nbsp;&nbsp;&nbsp;CTC不仅解决了以上的问题，还基于给定的输入序列$X$得出了$Y$的概率分布，可通过使用该概率分布推断可能的输出或评估输出的概率。<br>&nbsp;&nbsp;&nbsp;&nbsp;目标：<br>1)损失函数:驱使概率分布更接近准确的输出，计算Y在给定输入X下的概率分布($P(Y|X)$)<br>2)推论:最大化$P(Y|X)$，即<br>$$Y^* &#x3D; \arg\max_{Y}P(Y|X)$$</p>
<h2 id="时序对齐"><a href="#时序对齐" class="headerlink" title="时序对齐"></a>时序对齐</h2><p>&nbsp;&nbsp;&nbsp;&nbsp;论文举例 cat：<br>$$input(X): [x1, x2, x3, x4, x5, x6]\<br>alignment:  [ c,  c,  a,  a,  a,  t]\<br>output(Y):  [ c,      a,          t]$$</p>
<p>&nbsp;&nbsp;&nbsp;&nbsp;实现时序对齐的几个问题:<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;1)输入和输出可能无法完全对应，比如输入有长时间的静音<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;2)[h,h,e,l,l,l,o]可能会被识别成helo，实际为hello</p>
<p>&nbsp;&nbsp;&nbsp;&nbsp;为实现时序对齐，解决上述问题，论文提出的方案(remarkable):<br>$$引入空白字符\epsilon$$<br>&nbsp;&nbsp;&nbsp;&nbsp;作用是指代静音，区分字符相似的单词(如hello和helo)<br>&nbsp;&nbsp;&nbsp;&nbsp;基于此，hello的输入序列可以表示为[h,h,e,$\epsilon$,$\epsilon$,l,l,l,$\epsilon$,l,l,o]，去除重复字符后为[h,e,$\epsilon$,l,$\epsilon$,l,o]，去除空白字符后表示为[h,e,l,l,o]</p>
<h2 id="损失函数"><a href="#损失函数" class="headerlink" title="损失函数"></a>损失函数</h2><p>&nbsp;&nbsp;&nbsp;&nbsp;CTC使用RNN作为训练网络，在每一步都会生成输出序列的概率分布，其中对于每一对$(X,Y)$，其条件概率为：<br>$$p(Y|X) &#x3D; \sum_{A\in A_{X,Y}} {\prod_{t&#x3D;1}^T p_t(a_t|X)}$$<br>&nbsp;&nbsp;&nbsp;&nbsp;CTC loss的计算十分费时，论文使用动态规划对计算过程进行了优化，合并同一time step的相同输出，如下图所示:<br><img src="/2021/08/29/ASR-CTC/CTC_dp.png"><br>&nbsp;&nbsp;&nbsp;&nbsp;CTC算法的预测序列为<br>$$Z &#x3D; [\epsilon, y_1, \epsilon, y_2, \epsilon, y_3, …, \epsilon, y_U, \epsilon]$$</p>
<p>&nbsp;&nbsp;&nbsp;&nbsp;当前字符为空白字符$\epsilon$时，下一个字符有两种可能:空白字符或$Z$序列中的下一个字符。<br>&nbsp;&nbsp;&nbsp;&nbsp;当前字符不是空白字符时，下一个字符有三种可能:当前字符、空白字符或$Z$序列中的下一个字符。<br>&nbsp;&nbsp;&nbsp;&nbsp;下图为CTC的计算过程:<br><img src="/2021/08/29/ASR-CTC/CTC_compute.png"></p>
<p>&nbsp;&nbsp;&nbsp;&nbsp;基于动态规划，可用前向和后向概率计算CTC的loss，计算过程可参考[2]。<br>&nbsp;&nbsp;&nbsp;&nbsp;对于训练集D，CTC的loss计算可归纳为下式:<br>$$\sum_{(X,Y)\in D}{-\log p(Y|X)}$$<br>&nbsp;&nbsp;&nbsp;&nbsp;参考博客[3]，CTC的loss函数求解如下图所示:<br><img src="/2021/08/29/ASR-CTC/CTC_loss.png"><br>&nbsp;&nbsp;&nbsp;&nbsp;其中的z就是博客前半部分讨论的y，<br><img src="/2021/08/29/ASR-CTC/CTC_loss_def.png"></p>
<h2 id="推论"><a href="#推论" class="headerlink" title="推论"></a>推论</h2><p>&nbsp;&nbsp;&nbsp;&nbsp;由于直接暴力计算，$𝒑(𝒛│𝒙)$的复杂度非常高，论文中提及了greedy search和beam search两种方法[4]：<br>&nbsp;&nbsp;&nbsp;&nbsp;greedy search:每个时间片均取该时间片概率最高的节点作为输出<br>&nbsp;&nbsp;&nbsp;&nbsp;beam search:Beam Search是寻找全局最优值和Greedy Search在查找时间和模型精度的一个折中</p>
<h2 id="CTC与HMM"><a href="#CTC与HMM" class="headerlink" title="CTC与HMM"></a>CTC与HMM</h2><p>&nbsp;&nbsp;&nbsp;&nbsp;作者借鉴HMM的Forward-Backward算法思路，利用动态规划算法求解[3]。</p>
<h2 id="CTC缺陷"><a href="#CTC缺陷" class="headerlink" title="CTC缺陷"></a>CTC缺陷</h2><p>&nbsp;&nbsp;&nbsp;&nbsp;CTC假设每个时间片之间是相互独立，这种假设使输出丢失了时间片之间的语义信息，因此在CTC后部添加语言模型可有一定的优化效果。</p>
<h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><p>[1] <a href="https://distill.pub/2017/ctc/">https://distill.pub/2017/ctc/</a> Sequence Modeling With CTC<br>[2] <a href="https://xiaodu.io/ctc-explained/">https://xiaodu.io/ctc-explained/</a><br>[3] <a href="https://xiaodu.io/ctc-explained/">https://xiaodu.io/ctc-explained/</a><br>[4] <a href="https://zhuanlan.zhihu.com/p/42719047">https://zhuanlan.zhihu.com/p/42719047</a></p>
]]></content>
      <tags>
        <tag>ASR</tag>
        <tag>ASR-Classic</tag>
        <tag>Loss</tag>
      </tags>
  </entry>
  <entry>
    <title>ASR 噪声下的结构化抽取评测：从文本相似到实体归因</title>
    <url>/2026/06/10/ASR-Noise-Structured-Extraction-Evaluation/</url>
    <content><![CDATA[<p>在 ASR 输出上做 NER 或结构化抽取，不能只把它当成普通文本任务。语音转写会带来插入、删除、替换、同音错误和边界错位，导致实体在参考文本和识别文本中的位置天然不稳定。</p>
<p>如果仍然用严格字符 span 评测，很多可接受的识别会被误判为错误，真正严重的问题反而可能被掩盖。</p>
<span id="more"></span>

<h2 id="ASR-噪声改变了评测对象"><a href="#ASR-噪声改变了评测对象" class="headerlink" title="ASR 噪声改变了评测对象"></a>ASR 噪声改变了评测对象</h2><p>普通 NER 评测默认输入文本是稳定的，实体 span 可以精确定位。ASR 场景里，输入本身就是模型输出，错误会先发生在转写阶段。</p>
<p>例如一个实体被识别成同音近似词，结构化抽取模型可能仍能恢复语义；也可能 ASR 多插入几个字，导致后续 span 全部错位。严格按字符位置对齐，会把这些情况混在一起。</p>
<p>因此评测目标应该从“span 是否完全相等”扩展为“关键信息是否被正确恢复，以及错误来自哪里”。</p>
<h2 id="两阶段评测更稳"><a href="#两阶段评测更稳" class="headerlink" title="两阶段评测更稳"></a>两阶段评测更稳</h2><p>第一阶段评估 ASR 文本本身：WER、CER、关键词召回、数字和专名错误类型。</p>
<p>第二阶段评估结构化抽取结果：实体类型是否正确，实体值是否可接受，归一化结果是否一致，缺失和误报分别是多少。</p>
<p>这两阶段要能关联。否则抽取失败时，很难判断是 ASR 没听对，还是抽取模型没有理解。</p>
<h2 id="模糊匹配不是放松标准"><a href="#模糊匹配不是放松标准" class="headerlink" title="模糊匹配不是放松标准"></a>模糊匹配不是放松标准</h2><p>ASR 噪声下需要允许局部模糊匹配，但这不等于随意放宽标准。合理的做法是把匹配规则显式化：</p>
<ul>
<li>文本规范化后比较；</li>
<li>在局部窗口内匹配实体；</li>
<li>对同音、数字读法和简繁差异单独处理；</li>
<li>对不同实体类型使用不同容忍度；</li>
<li>输出 exact、partial、normalized 三类结果。</li>
</ul>
<p>这样可以保留评测的可解释性。</p>
<h2 id="错误归因比单个-F1-更有用"><a href="#错误归因比单个-F1-更有用" class="headerlink" title="错误归因比单个 F1 更有用"></a>错误归因比单个 F1 更有用</h2><p>实体 F1 能说明总体效果，但不能指导修复。更有价值的是错误归因：</p>
<ul>
<li>ASR 删除导致实体缺失；</li>
<li>ASR 替换导致实体值错误；</li>
<li>文本规范化导致格式不一致；</li>
<li>抽取模型漏召回；</li>
<li>抽取模型误报；</li>
<li>后处理归一化失败。</li>
</ul>
<p>有了归因，后续才知道应该改 ASR、改抽取模型、改 normalizer，还是改评测规则。</p>
<h2 id="开源工具形态"><a href="#开源工具形态" class="headerlink" title="开源工具形态"></a>开源工具形态</h2><p>一个通用评测工具可以设计成三层输入：</p>
<ol>
<li>REF：参考文本和实体标注；</li>
<li>HYP：ASR 文本和抽取结果；</li>
<li>normalization config：实体类型、匹配窗口、归一化规则。</li>
</ol>
<p>输出包括样本级报告、实体级 precision&#x2F;recall&#x2F;F1、错误归因分布和可人工复核的 bad case 列表。</p>
<p>这类工具不需要绑定特定模型，反而更适合开源：它提供的是评测语言，而不是某个单点模型。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>ASR 噪声下的结构化抽取评测，核心是把文本错误、实体错误和归一化错误拆开。只有错误类型被看见，系统优化才有方向。</p>
<h2 id="Alignment-aware-scorer"><a href="#Alignment-aware-scorer" class="headerlink" title="Alignment-aware scorer"></a>Alignment-aware scorer</h2><p>Noisy ASR text should be scored with an alignment-aware extractor. The scorer first aligns reference and hypothesis text, then evaluates entity value, entity type, local window match, normalization result, and error source.</p>
<p>This avoids treating every span mismatch as an extraction failure. Some misses come from ASR substitution or deletion, some from normalization, and some from the downstream extractor. The repair path is different for each category.</p>
]]></content>
      <tags>
        <tag>ASR</tag>
        <tag>Open Source</tag>
        <tag>NER</tag>
        <tag>Evaluation</tag>
      </tags>
  </entry>
  <entry>
    <title>ASR-LAS-model</title>
    <url>/2021/08/24/ASR-LAS-model/</url>
    <content><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;论文《Listen Attend and Spell》LAS模型简介及代码复现</p>
<span id="more"></span>
<p>&nbsp;&nbsp;&nbsp;&nbsp;论文地址: <a href="https://arxiv.org/abs/1508.01211v2">https://arxiv.org/abs/1508.01211v2</a></p>
<h3 id="论文核心思想"><a href="#论文核心思想" class="headerlink" title="论文核心思想"></a>论文核心思想</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;语音识别模型，模型分为两个部分：listener和speller：<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;listener是三角形的pyramidal bidirecional lstm。<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;spller是基于attention的decoder,论文的label是以字符为单位的。</p>
<p>&nbsp;&nbsp;&nbsp;&nbsp;Encoder:<br>&nbsp;&nbsp;&nbsp;&nbsp;Decoder:</p>
<h3 id="论文复现代码参考"><a href="#论文复现代码参考" class="headerlink" title="论文复现代码参考"></a>论文复现代码参考</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;pytorch版: <a href="https://github.com/AzizCode92/Listen-Attend-and-Spell-Pytorch">https://github.com/AzizCode92/Listen-Attend-and-Spell-Pytorch</a></p>
<h3 id="LAS模型原理图"><a href="#LAS模型原理图" class="headerlink" title="LAS模型原理图"></a>LAS模型原理图</h3><p><img src="/2021/08/24/ASR-LAS-model/LAS_flow.png"></p>
<h3 id="References"><a href="#References" class="headerlink" title="References"></a>References</h3><p>[1]《<a href="https://zhuanlan.zhihu.com/p/80967163%E3%80%8B">https://zhuanlan.zhihu.com/p/80967163》</a></p>
]]></content>
      <tags>
        <tag>ASR</tag>
        <tag>ASR-Classic</tag>
        <tag>Deep-learning</tag>
        <tag>Attention</tag>
      </tags>
  </entry>
  <entry>
    <title>ASR-Multilingual</title>
    <url>/2022/04/22/ASR-Multilingual/</url>
    <content><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;多语种语音识别overview</p>
<span id="more"></span>

<h2 id="为什么需要多语种语音识别"><a href="#为什么需要多语种语音识别" class="headerlink" title="为什么需要多语种语音识别?"></a>为什么需要多语种语音识别?</h2><p>1、优化低资源语种的识别效果: 世界上大部分的语种存在数据匮乏的现象, 多语种语音识别具备一定潜力提升低资源语音识别的效果, 如采用迁移学习,<br>   基于识别效果较好的语种模型或是多语种模型进行调优。【关键】</p>
<p>2、优化口音识别：实际的普通话或英文识别应用场景包含较多的方言口音识别问题反馈，实现自由说需针对性优化口音问题。</p>
<p>3、优化模型部署流程：随着识别语种的不断增加，部署和维护每一种语种的几个模型会变得很麻烦，多语种免切换可在一定程度上优化系统性能。</p>
<h2 id="语音识别的技术发展"><a href="#语音识别的技术发展" class="headerlink" title="语音识别的技术发展"></a>语音识别的技术发展</h2><p>GMM-HMM -&gt; DNN-HMM -&gt; RNN-E2E -&gt; attention-E2E -&gt; joint network<br>本质还是神经网络革新带动技术进步。<br>当前传统声学模型+语言模型的识别系统仍然在工业界活跃, 主要是传统的识别系统每个组件更独立, 有更丰富的调优方式。相对来说端到端语音识别的调优方式仍在发展, 但已是未来趋势—做传统的赶紧学。</p>
<h2 id="现有的多语种识别技术路线"><a href="#现有的多语种识别技术路线" class="headerlink" title="现有的多语种识别技术路线"></a>现有的多语种识别技术路线</h2><p>1、跨语种迁移学习(cross-lingual transfer learning)<br>2、多任务学习(multi-task learning)<br>3、自监督学习(self-supervised learning)…我觉得和1有一些交集, 本质是预训练、nlp带火的<br>4、音素集共享(common phone)<br>5、可能还有吧。。。有些上面几个可以囊括了</p>
<h3 id="References"><a href="#References" class="headerlink" title="References"></a>References</h3><p>[1] <a href="https://arxiv.org/abs/2202.12576v1">https://arxiv.org/abs/2202.12576v1</a></p>
]]></content>
      <tags>
        <tag>ASR</tag>
        <tag>ASR Low-resource</tag>
        <tag>ASR Multilingual</tag>
      </tags>
  </entry>
  <entry>
    <title>ASR 数据质量流水线：伪标签、切分与可追溯评测</title>
    <url>/2026/06/10/ASR-Data-Quality-Pipeline-Open-Source/</url>
    <content><![CDATA[<p>ASR 系统的上限经常不是模型结构先决定的，而是数据质量先决定的。开源模型可以替换，训练框架可以迁移，但如果音频切分、文本规范化、伪标签和评测口径不可靠，最后得到的指标会很快失真。</p>
<p>一个可靠的 ASR 数据质量流水线，应该把“数据是否可训练”和“模型是否有效”拆开处理。</p>
<span id="more"></span>

<h2 id="四层质量控制"><a href="#四层质量控制" class="headerlink" title="四层质量控制"></a>四层质量控制</h2><p>第一层是音频质量。采样率、通道数、时长、静音比例、截断、重复片段、过强背景声都会影响训练。这里不需要一开始就追求复杂模型，先做统计分布、异常样本导出和可复核列表。</p>
<p>第二层是文本质量。数字读法、标点、大小写、口语填充词、特殊符号、空文本都要有统一规范。否则模型学到的是标注风格差异，而不是语音和文字的对应关系。</p>
<p>第三层是对齐质量。长音频切分、片段边界、token 置信度、CTC alignment 或 forced alignment 都属于这一层。片段边界错了，训练样本看起来完整，实际监督已经偏移。</p>
<p>第四层是仲裁质量。多模型一致、置信度阈值、规则过滤和人工抽检应该输出 reason code。保留一个样本不是因为“看起来不错”，而是因为它满足了可解释条件。</p>
<h2 id="多模型一致不是金标准"><a href="#多模型一致不是金标准" class="headerlink" title="多模型一致不是金标准"></a>多模型一致不是金标准</h2><p>两个 ASR 模型输出一致，并不代表答案正确。它们可能共享训练数据偏差，也可能在低能量片段、噪声片段、同音词上犯相同错误。</p>
<p>所以多模型一致更适合作为“候选保留信号”，不能作为唯一真值。更好的策略是把一致性与其他证据组合：音频能量、片段边界、token 置信度、语言模型困惑度、实体词命中情况和抽检反馈。</p>
<h2 id="切分比想象中重要"><a href="#切分比想象中重要" class="headerlink" title="切分比想象中重要"></a>切分比想象中重要</h2><p>ASR 数据经常从长音频切成短片段。切分过短会丢上下文，切分过长会增加解码和训练负担；边界切在词中间，会制造无法学习的样本。</p>
<p>切分策略至少要记录：</p>
<ul>
<li>原始音频 id；</li>
<li>起止时间；</li>
<li>是否经过重采样；</li>
<li>是否有重叠窗口；</li>
<li>切分依据来自 VAD、固定窗口还是外部标注；</li>
<li>文本是否和该片段严格对应。</li>
</ul>
<p>没有这些信息，后续排查只能回到原始文件重新猜。</p>
<h2 id="评测要拆错误类型"><a href="#评测要拆错误类型" class="headerlink" title="评测要拆错误类型"></a>评测要拆错误类型</h2><p>WER&#x2F;CER 是必要指标，但不是充分指标。对于很多语音系统，更关键的是专名、数字、地址、术语、时间表达和关键词的召回。</p>
<p>建议把评测拆成三层：</p>
<ol>
<li>普通转写指标：WER、CER、句级准确率；</li>
<li>关键信息指标：实体词 precision、recall、F1；</li>
<li>错误归因指标：插入、删除、替换、边界错位、规范化错误。</li>
</ol>
<p>这样可以区分模型真的听错了，还是文本规范化或后处理把结果改坏了。</p>
<h2 id="开源实现建议"><a href="#开源实现建议" class="headerlink" title="开源实现建议"></a>开源实现建议</h2><p>一个可复用的开源流水线可以包含这些模块：</p>
<ul>
<li>audio profiler：输出音频分布和异常样本；</li>
<li>text normalizer：集中管理文本规范化规则；</li>
<li>segment validator：检查切分边界和文本覆盖；</li>
<li>pseudo-label arbiter：融合多模型和置信度信号；</li>
<li>eval reporter：按错误类型输出报告；</li>
<li>sample browser：把坏样本导出为可人工复核的列表。</li>
</ul>
<p>重点不是一次性把所有规则写死，而是让每个过滤决策都能追溯。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>ASR 数据流水线的目标不是“尽量多清洗”，而是建立可解释的数据决策系统。每个样本为什么进入训练集、为什么被丢弃、为什么指标变化，都应该能从日志和报告里找到依据。</p>
<h2 id="Data-card-与-reason-code"><a href="#Data-card-与-reason-code" class="headerlink" title="Data card 与 reason code"></a>Data card 与 reason code</h2><p>A reusable ASR data pipeline should generate a data card for every sample. The card can include audio profile, text-normalization result, segmentation boundary, pseudo-label source, model disagreement, rule hits, manual-review status, and final action.</p>
<p>The important field is the reason code. Keeping, dropping, or rewriting a sample should be explainable later. Without that audit trail, a training regression cannot be traced back to a data decision.</p>
]]></content>
      <tags>
        <tag>ASR</tag>
        <tag>Open Source</tag>
        <tag>Evaluation</tag>
        <tag>Data</tag>
      </tags>
  </entry>
  <entry>
    <title>ASR-preprocess</title>
    <url>/2021/08/24/ASR-preprocess/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>&nbsp;&nbsp;&nbsp;&nbsp;语音信号预处理相关流程简介, 包括预加重、分帧加窗、DFT、FBank等</p>
<span id="more"></span>
<h2 id="语音信号提取流程"><a href="#语音信号提取流程" class="headerlink" title="语音信号提取流程"></a>语音信号提取流程</h2><p><img src="/2021/08/24/ASR-preprocess/preprocess_flow.png"></p>
<p>(python代码可参考References部分的博客)</p>
<h3 id="预加重"><a href="#预加重" class="headerlink" title="预加重"></a>预加重</h3><h4 id="Question-为什么需要预加重？"><a href="#Question-为什么需要预加重？" class="headerlink" title="Question:为什么需要预加重？"></a>Question:为什么需要预加重？</h4><p>&nbsp;&nbsp;&nbsp;&nbsp;提高信号高频部分的能量,高频信号在传递过程中,衰减较快,包含更多的语音特征,n与n-1的变化幅度更大。</p>
<h4 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h4><p>&nbsp;&nbsp;&nbsp;&nbsp;预加重是一个一阶高通滤波器，给定时域输入信号$x[n]$,预加重之后的信号为<br>$$y[n] &#x3D; x[n] - \alpha x[n-1], \alpha \in [0.9, 1.0]$$<br>&nbsp;&nbsp;&nbsp;&nbsp;当信号为低频信号时，x变化较慢，$x[n]$与$x[n-1]$较为接近，低频信号的幅度会被大大抑制，同理，可保证高频信号特征的传递。</p>
<h3 id="分帧加窗"><a href="#分帧加窗" class="headerlink" title="分帧加窗"></a>分帧加窗</h3><h4 id="Question-为什么需要分帧？"><a href="#Question-为什么需要分帧？" class="headerlink" title="Question:为什么需要分帧？"></a>Question:为什么需要分帧？</h4><p>&nbsp;&nbsp;&nbsp;&nbsp;1、语音信号为非平稳信号，统计属性随时间变化。<br>&nbsp;&nbsp;&nbsp;&nbsp;2、语音信号同时具有短时平稳特性，一个声母&#x2F;韵母的发音往往持续几十到几百毫秒。<br>&nbsp;&nbsp;&nbsp;&nbsp;3、语音识别以较小发音单元的识别进行，通常为音素、字或字节，需要滑动窗来提取短时片段。</p>
<p>&nbsp;&nbsp;&nbsp;&nbsp;对于采样率为16kHz的信号，帧长、帧移一般为25ms、10ms，即400和160个采样点<br>&nbsp;&nbsp;&nbsp;&nbsp;帧长是一个窗口的长度，帧移是帧长减去连续两帧之间重叠的部分(overlap)。</p>
<h4 id="原理-1"><a href="#原理-1" class="headerlink" title="原理"></a>原理</h4><p>&nbsp;&nbsp;&nbsp;&nbsp;分帧的过程，在时域上，即用一个窗函数和原始信号进行相乘:<br>$$y[n] &#x3D; w[n]x[n]$$<br>$w[n]$称为窗函数，常用的窗函数有<br>矩形窗：<br>$$w[n]&#x3D;\left{ \begin{aligned}{1,n\in [0,L-1] \ 0,otherwise}\end{aligned}\right. $$<br>汉明窗(Hamming):<br>$$w[n]&#x3D;\left{ \begin{aligned}{0.54-0.46\cos\frac{2\pi n}{L} ,n\in [0,L-1] \ 0,otherwise}\end{aligned}\right. $$</p>
<p>&nbsp;&nbsp;&nbsp;&nbsp;使用汉明窗:加窗的过程，实际上是在时域上将信号截断，窗函数与信号在时域相乘，就等于对应的频域表示进行卷积(*)，矩形窗主瓣窄，但是旁瓣较大，将其与原信号的频域表示进行卷积，就会导致频率泄漏</p>
<h3 id="DFT"><a href="#DFT" class="headerlink" title="DFT"></a>DFT</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;将上一步分帧之后的语音帧，通过快速傅里叶变换由时域变换到频域，取DFT系数的模，得到谱特征,在此不做详细描述，可参考下列文章(压箱底系列)<br>&nbsp;&nbsp;&nbsp;&nbsp;DFT: <a href="https://zhuanlan.zhihu.com/p/77347644">https://zhuanlan.zhihu.com/p/77347644</a><br>&nbsp;&nbsp;&nbsp;&nbsp;C语言-FFT蝶形运算：<a href="https://zhuanlan.zhihu.com/p/135259438">https://zhuanlan.zhihu.com/p/135259438</a><br>&nbsp;&nbsp;&nbsp;&nbsp;傅里叶变换的意义: <a href="https://blog.csdn.net/guyuealian/article/details/72817527">https://blog.csdn.net/guyuealian/article/details/72817527</a><br>&nbsp;&nbsp;&nbsp;&nbsp;复数的物理意义: <a href="https://www.zhihu.com/question/23234701">https://www.zhihu.com/question/23234701</a><br>&nbsp;&nbsp;&nbsp;&nbsp;信号频域与时域的区别:<a href="https://www.zhihu.com/question/21817515">https://www.zhihu.com/question/21817515</a></p>
<h3 id="FBank"><a href="#FBank" class="headerlink" title="FBank"></a>FBank</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;DFT得到了每个频带上信号的能量，但是人耳对频率的感知不是等间隔的，近似于对数函数</p>
<p>&nbsp;&nbsp;&nbsp;&nbsp;将线性频率转换为梅尔频率，梅尔频率和线性频率转换关系<br>$$mel(f)&#x3D;2595\log_{10}(1+\frac{f}{700})$$<br><img src="/2021/08/24/ASR-preprocess/mel_filter.png"></p>
<h3 id="梅尔滤波器组设计"><a href="#梅尔滤波器组设计" class="headerlink" title="梅尔滤波器组设计"></a>梅尔滤波器组设计</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;(1)确定滤波器组个数P<br>&nbsp;&nbsp;&nbsp;&nbsp;(2)根据采样率fs，DFT点数N，滤波器个数P，在梅尔域上等间隔的产生每个滤波器的起始频率、中间频率和截至频率，注意，上一个滤波器的中间频率为下一个滤波器的起始频率（存在overlap）<br>&nbsp;&nbsp;&nbsp;&nbsp;(3)将梅尔域上每个三角滤波器的起始、中间和截止频率转换线性频率域，并对DFT之后的谱特征进行滤波，得到P个滤波器组能量，进行log 操作，得到Fbank特征</p>
<h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><p>[1]《语音信号预处理及python代码实现博客:<br>&nbsp;&nbsp;&nbsp;&nbsp;<a href="https://www.cnblogs.com/lxp-never/p/10918590.html#blogTitle7%E3%80%8B">https://www.cnblogs.com/lxp-never/p/10918590.html#blogTitle7》</a></p>
]]></content>
      <tags>
        <tag>ASR</tag>
        <tag>Preprocess</tag>
      </tags>
  </entry>
  <entry>
    <title>CCA application in SSVEP frequency detection</title>
    <url>/2019/07/15/CCA-application-in-SSVEP-frequency-detection/</url>
    <content><![CDATA[<h2 id="典型相关分析在稳态视觉诱发电位频率识别方面的应用"><a href="#典型相关分析在稳态视觉诱发电位频率识别方面的应用" class="headerlink" title="典型相关分析在稳态视觉诱发电位频率识别方面的应用"></a>典型相关分析在稳态视觉诱发电位频率识别方面的应用</h2><h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;随着脑机接口的快速发展，典型相关分析在稳态视觉诱发电位频率识别方面占据着重要的地位，由于脑电信号非线性的特征，使得机器学习算法在该领域的使用不具备优势，而比较信号同向变化相似率的CCA算法却取得了十分出色的效果。本文将对于CCA在SSVEP方面的应用发展以及优缺点进行介绍，希望为国内的脑机接口博客发展做出贡献。</p>
<span id="more"></span>
<h3 id="Question-1-什么是CCA？"><a href="#Question-1-什么是CCA？" class="headerlink" title="Question 1: 什么是CCA？"></a>Question 1: 什么是CCA？</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;CCA是一个多变量的统计方法，当有两组数据时使用，可能有一些潜在的相关性。CCA将普通的相关性扩展到两组变量。首先，CCA为两个集合找到一对称为正则变量的线性组合，使两个正则变量之间的相关性最大化。然后找到第二对，与第一对正则变量不相关，但相关性次之。构造正则变量的过程一直持续下去，直到正则变量对的数目等于较小集合中变量的数目。系数描述了两个集合之间的相关关系。但脑电信号中应用时只考虑了最大相关系数[1]。</p>
<h3 id="Question-2-什么是SSVEP？"><a href="#Question-2-什么是SSVEP？" class="headerlink" title="Question 2: 什么是SSVEP？"></a>Question 2: 什么是SSVEP？</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;稳态状态视觉诱发电位(SSVEP)可定义为由快速重复的视觉刺激诱发的周期性诱发电位，其频率一般大于6hz。SSVEP由若干离散的频率分量组成。与ssvep相关的频率范围通常包括视觉刺激的基频及其谐波[1]。<br>&nbsp;&nbsp;&nbsp;&nbsp;SSVEP采集时以不同频率的光标闪烁刺激大脑产生相应频率的电信号，如下图所示:<br><img src="/2019/07/15/CCA-application-in-SSVEP-frequency-detection/SSVEP_collect.jpg"><br>&nbsp;&nbsp;&nbsp;&nbsp;SSVEP信号经过傅里叶变换后，可在刺激信号及其谐波处看到明显的尖峰，如下图所示:</p>
<p><img src="/2019/07/15/CCA-application-in-SSVEP-frequency-detection/SSVEP_signal.png"></p>
<h4 id="CCA在SSVEP中的应用"><a href="#CCA在SSVEP中的应用" class="headerlink" title="CCA在SSVEP中的应用"></a>CCA在SSVEP中的应用</h4><p>&nbsp;&nbsp;&nbsp;&nbsp;CCA使用了两种信号，一个是用于刺激大脑的闪烁信号，另一个是大脑相应产生的SSVEP信号。<br>&nbsp;&nbsp;&nbsp;&nbsp;闪烁信号由于具有特定的频率，因此可根据傅里叶级数分解为$\sin 2{\pi}ft$, $\cos 2{\pi}ft$, $\sin 4{\pi}ft$…<br>&nbsp;&nbsp;&nbsp;&nbsp;如下式总结：<br>$$y(t) &#x3D; { y1(t) y2(t) y3(t) y4(t) y5(t) y6(t)} &#x3D; { \sin 2{\pi}ft \cos 2{\pi}ft \sin 4{\pi}ft \cos 4{\pi}ft \sin 6{\pi}ft \cos 6{\pi}ft}$$<br>$$t&#x3D;\frac1S, \frac2S, …, \frac{T}{S}, T是采样点数, S是采样率 $$<br>&nbsp;&nbsp;&nbsp;&nbsp;高阶谐波可忽略，这里取六个谐波信号。<br>&nbsp;&nbsp;&nbsp;&nbsp;SSVEP信号源自大脑上的不同通道，在[1]中，选择的通道为B2, Oz, O2, POz, D2, O1, PO7和PO1,共八个。<br>&nbsp;&nbsp;&nbsp;&nbsp;CCA的结构如下图所示：<br><img src="/2019/07/15/CCA-application-in-SSVEP-frequency-detection/CCA.png"><br>公式如下图所示（太复杂，不想用markdown写了）<br><img src="/2019/07/15/CCA-application-in-SSVEP-frequency-detection/CCAFormula.png"><br>&nbsp;&nbsp;&nbsp;&nbsp;将分母设为1,该式可变为一个约束优化问题，可以用广义特征值分解的方式求解，具体的求解流程可以参考CCA的<a href="https://www.cnblogs.com/pinard/p/6288716.html">其他博客</a>。<br>&nbsp;&nbsp;&nbsp;&nbsp;本文只做大体框架的梳理。</p>
<h3 id="Question-3-CCA的缺陷？"><a href="#Question-3-CCA的缺陷？" class="headerlink" title="Question 3: CCA的缺陷？"></a>Question 3: CCA的缺陷？</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;典型相关分析自 2006 年 Lin 等人将其应用于脑机接口以来，因其结合了空间滤波、特征提取及分类功能而深受研究者的喜爱，并且在 SSVEP 信号处理中有着极为优秀的精度。但该算法仍然存在如下问题：<br>&nbsp;&nbsp;&nbsp;&nbsp;1）相位漂移问题。每个受试者对于同种视觉刺激具有不同的反应时滞，从而产生了脑电数据的相位漂移问题，当相位漂移问题存在时，数据训练往往会产生较大的偏差。<br>&nbsp;&nbsp;&nbsp;&nbsp;2） CCA 主要用于线性信号的处理，而脑电信号为非线性信号。在 CCA 的相关实验中，假设脑电信号为刺激频率的线性输出，但实际混杂了部分环境噪声及伪迹。CCA 在 2s 以上的长时间片中的表现极为优秀，但因为其未考虑脑电信号非线性的特性，在 1s 以内短时间片处理时仍存在不足之处。<br>&nbsp;&nbsp;&nbsp;&nbsp;近年来，研究者针对以上问题提出了很多不同的优化方案，包括使用脑电数据替代刺激信号作为参考信号以及利用数学公式计算受试者的相位偏移等。</p>
<h3 id="CCA-extend"><a href="#CCA-extend" class="headerlink" title="CCA extend"></a>CCA extend</h3><h4 id="DCCA-深度典型相关分析算法"><a href="#DCCA-深度典型相关分析算法" class="headerlink" title="DCCA(深度典型相关分析算法)"></a>DCCA(深度典型相关分析算法)</h4><p>&nbsp;&nbsp;&nbsp;&nbsp;关于非线性信号处理问题，研究者们尝试加入非线性算法进行优化处理。而近年来获得研究者们青睐的深度学习算法具备着较为优秀的非线性数据处理能力，如下图所示，通过多层神经网络表达典型相关分析算法的基础结构，并在此基础加入非线性处理相关算法，构建基于皮尔逊相关系数的特征提取模型，从而达到非线性信号分析处理的效果。<br>&nbsp;&nbsp;&nbsp;&nbsp;深度典型相关分析算法将网络模型分为两部分，一部分为参考信号的训练网络，一部分为待检测脑电信号的训练网络，网络结构的核心部分为典型相关分析算法的多层网络实现。深度典型相关分析算法利用训练数据获取单个受试者的空间滤波权值矩阵，因该权值矩阵经过多层网络，因此获得了更好的非线性优化效果，最后单个受试者的相关脑电数据在测试时即可获得较好的优化效果[2]。<br>&nbsp;&nbsp;&nbsp;&nbsp;深度典型相关分析的缺陷是，其使用了深度神经网络这一需要大量训练数据的算法结构，而实际采集时脑电数据获取困难，数据量较少，另外，深度神经网络虽解决了 CCA 算法中存在的非线性信号精度丢失问题，但对于相位漂移问题缺乏较好的优化措施。<br>DCCA原理图：<br><img src="/2019/07/15/CCA-application-in-SSVEP-frequency-detection/DCCA.png"></p>
<h4 id="TRCA-任务组成相关分析算法"><a href="#TRCA-任务组成相关分析算法" class="headerlink" title="TRCA(任务组成相关分析算法)"></a>TRCA(任务组成相关分析算法)</h4><p>&nbsp;&nbsp;&nbsp;&nbsp;TRCA 算法从脑电数据本身出发，将脑电数据视为任务相关及任务不相关两部分，通过最大化同一类脑电数据的协方差，构建了能够提取任务相关成分的权值矩阵，即实现了一个性能优秀的空间滤波数学模型，从而在利用皮尔逊相关系数进行分类时可以获得更为出色的优化效果，其处理流程如下图所示[3]。<br>&nbsp;&nbsp;&nbsp;&nbsp;TRCA 算法的参考信号使用了多通道脑电数据的均值，使得待检测脑电数据与参考信号可通过同一空间滤波权值矩阵提取任务相关特征，从而增大参考信号与检测信号的相关程度。实际算法处理过程中，需基于一定数量的训练数据获得空间滤波所需的权值矩阵以进行有效的任务相关特征提取。<br>&nbsp;&nbsp;&nbsp;&nbsp;相对于 CCA 算法，TRCA 在短时间片数据处理中具有极大的优势。相较深度典型相关分析算法，TRCA 算法的训练数据需求相对较少，并且训练效果好，无需构建复杂的网络模型。<br>TRCA算法原理图:</p>
<img src="/2019/07/15/CCA-application-in-SSVEP-frequency-detection/TRCA.png" width=256 height=256/>

<h3 id="References"><a href="#References" class="headerlink" title="References"></a>References</h3><p>[1]《Frequency Recognition Based on Canonical Correlation Analysis for SSVEP-Based BCIs》<br>[2]《Deep canonical correlation analysis》<br>[3]《Enhancing detection of SSVEPs for a high­speed brain speller using task­related component analysis》</p>
]]></content>
      <tags>
        <tag>CCA</tag>
        <tag>EEG</tag>
      </tags>
  </entry>
  <entry>
    <title>Agentic Coding 工程治理：多模型协作的角色、权限与验证闭环</title>
    <url>/2026/06/10/Agentic-Coding-Governance/</url>
    <content><![CDATA[<p>代码生成工具越来越强，多模型协作也越来越常见。真正的问题已经不只是“哪个模型会写代码”，而是多个代理如何共享上下文、谁能写文件、谁负责审查、如何避免互相覆盖，以及如何验证最终结果。</p>
<p>Agentic Coding 的难点更像工程治理，而不是单纯模型能力。</p>
<span id="more"></span>

<h2 id="先分角色，再接工具"><a href="#先分角色，再接工具" class="headerlink" title="先分角色，再接工具"></a>先分角色，再接工具</h2><p>一个稳定的多代理流程，至少应该区分四类角色：</p>
<ul>
<li>explorer：只读探索代码，回答具体问题；</li>
<li>worker：负责明确文件范围内的实现；</li>
<li>reviewer：检查风险、回归和测试缺口；</li>
<li>integrator：合并结果、运行验证、决定是否发布。</li>
</ul>
<p>如果所有代理都能同时写同一批文件，冲突和回退风险会急剧上升。写入权限应该比读取权限更谨慎。</p>
<h2 id="单一写入者原则"><a href="#单一写入者原则" class="headerlink" title="单一写入者原则"></a>单一写入者原则</h2><p>多代理不是越并行越好。最容易失控的情况，是多个代理同时修改同一模块，却没有统一状态机。</p>
<p>一个实用原则是：同一文件或同一行为边界，在同一阶段只交给一个写入者。其他代理可以读、审、提建议，但不要并发改同一处。</p>
<p>如果确实要并行实现，写入范围必须物理隔离，例如一个负责测试，一个负责文档，一个负责某个独立模块。</p>
<h2 id="上下文要可复盘"><a href="#上下文要可复盘" class="headerlink" title="上下文要可复盘"></a>上下文要可复盘</h2><p>代理之间共享的不是聊天热情，而是结构化上下文。关键决策应该进入任务记录：</p>
<ul>
<li>要改什么行为；</li>
<li>不改什么边界；</li>
<li>已验证什么事实；</li>
<li>失败过哪些方案；</li>
<li>哪些测试必须通过；</li>
<li>当前还有什么风险。</li>
</ul>
<p>这些信息如果只留在对话里，下一轮代理很容易重复踩坑。</p>
<h2 id="MCP-与工具权限"><a href="#MCP-与工具权限" class="headerlink" title="MCP 与工具权限"></a>MCP 与工具权限</h2><p>MCP、浏览器控制、终端、GitHub 连接器都能扩展代理能力。能力越强，权限边界越重要。</p>
<p>建议把工具分成三类：</p>
<ol>
<li>read-only：搜索、读取、截图、日志查看；</li>
<li>scoped-write：只允许写指定目录或文件；</li>
<li>privileged：发布、删除、重置、凭证相关操作。</li>
</ol>
<p>默认给 read-only，需要写入时再按任务授权。这样可以减少无意副作用。</p>
<h2 id="验证闭环"><a href="#验证闭环" class="headerlink" title="验证闭环"></a>验证闭环</h2><p>Agentic Coding 的交付不能停在“模型说完成了”。至少要有三类验证：</p>
<ul>
<li>静态验证：格式、lint、类型、配置检查；</li>
<li>行为验证：测试、构建、端到端 smoke；</li>
<li>变更验证：diff 审阅、敏感信息扫描、发布前检查。</li>
</ul>
<p>如果代理修改了验证脚本本身，还要确认验证规则没有被放松。</p>
<h2 id="开源实践方向"><a href="#开源实践方向" class="headerlink" title="开源实践方向"></a>开源实践方向</h2><p>这个方向适合沉淀成可复用模板：</p>
<ul>
<li>任务计划格式；</li>
<li>代理角色说明；</li>
<li>写入范围声明；</li>
<li>审查 checklist；</li>
<li>发布前检查脚本；</li>
<li>失败记录和恢复流程。</li>
</ul>
<p>模型会变，工具会变，但这些治理结构相对稳定。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>Agentic Coding 的关键不是把多个模型接到一起，而是明确角色、权限、状态和验证。没有边界的多代理系统，只会把单模型的不确定性放大成协作层面的不确定性。</p>
<h2 id="Role-state-handoff"><a href="#Role-state-handoff" class="headerlink" title="Role-state handoff"></a>Role-state handoff</h2><p>Agentic coding workflows benefit from a small state machine: explore, propose, apply, review, verify, publish. Each state should define the allowed tools, write boundary, expected artifact, and handoff summary.</p>
<p>The handoff should preserve verified facts, rejected assumptions, touched files, test evidence, residual risks, and the next action. This is more reliable than preserving a raw transcript, because later agents need current state and evidence, not every intermediate turn.</p>
]]></content>
      <tags>
        <tag>AI</tag>
        <tag>Open Source</tag>
        <tag>Agent</tag>
        <tag>Coding</tag>
      </tags>
  </entry>
  <entry>
    <title>LLM 与语音模型推理服务：队列、流式与可观测性</title>
    <url>/2026/06/10/LLM-Speech-Inference-Serving-Observability/</url>
    <content><![CDATA[<p>推理服务的性能问题，很多时候不在单次 GPU forward，而在排队、预处理、batch 调度、流式输出、取消机制和可观测性之间。模型越大，单次推理越重要；系统越复杂，队列和状态越容易成为真正瓶颈。</p>
<p>vLLM、SGLang、Triton Inference Server、TensorRT-LLM 等开源生态，提供的是不同层级的解法。选型前要先拆清楚瓶颈在哪里。</p>
<span id="more"></span>

<h2 id="不要只看端到端耗时"><a href="#不要只看端到端耗时" class="headerlink" title="不要只看端到端耗时"></a>不要只看端到端耗时</h2><p>端到端耗时是结果，不是诊断。一个请求慢，可能慢在：</p>
<ul>
<li>等待队列；</li>
<li>tokenizer 或 audio frontend；</li>
<li>prefill；</li>
<li>decode；</li>
<li>网络传输；</li>
<li>后处理；</li>
<li>流式 flush；</li>
<li>取消请求没有及时生效。</li>
</ul>
<p>如果日志只记录总耗时，系统调优会很盲。至少要把 request queue time、prefill time、decode time、first token latency、p95&#x2F;p99 latency 和 GPU utilization 分开。</p>
<h2 id="vLLM-适合什么"><a href="#vLLM-适合什么" class="headerlink" title="vLLM 适合什么"></a>vLLM 适合什么</h2><p>vLLM 的重点是高吞吐 LLM serving，PagedAttention 和 continuous batching 是它的核心优势。它适合请求量较高、输入输出长度差异明显、需要动态 batch 的场景。</p>
<p>但 vLLM 不是所有问题的答案。若瓶颈在音频预处理、外部工具调用、复杂后处理或流式状态管理，仅替换引擎未必能带来明显收益。</p>
<h2 id="SGLang-的位置"><a href="#SGLang-的位置" class="headerlink" title="SGLang 的位置"></a>SGLang 的位置</h2><p>SGLang 更强调结构化生成和运行时控制。它适合把 prompt、约束解码、工具调用、并行分支和缓存复用组织成更可控的程序。</p>
<p>如果系统需要复杂推理流程，SGLang 这类运行时可以减少手写 glue code。但它也要求把生成过程抽象得更清晰，否则只是把复杂度换了位置。</p>
<h2 id="Triton-与底层引擎"><a href="#Triton-与底层引擎" class="headerlink" title="Triton 与底层引擎"></a>Triton 与底层引擎</h2><p>Triton 更像通用推理编排层，适合把预处理、模型、后处理拆成可观测组件。TensorRT-LLM 更偏底层引擎优化，依赖硬件、版本和模型结构匹配。</p>
<p>它们更适合明确瓶颈已经在底层执行路径的场景。如果请求排队、数据转换和状态管理没有先量化，过早下沉到引擎优化很容易浪费时间。</p>
<h2 id="语音模型的额外变量"><a href="#语音模型的额外变量" class="headerlink" title="语音模型的额外变量"></a>语音模型的额外变量</h2><p>语音模型比纯文本 LLM 多几类状态：</p>
<ul>
<li>音频 chunk 边界；</li>
<li>重叠窗口；</li>
<li>frontend 缓存；</li>
<li>partial hypothesis；</li>
<li>长音频切分；</li>
<li>实时输入的取消和刷新。</li>
</ul>
<p>这些状态如果没有清晰生命周期，会造成内存泄漏、延迟抖动或输出污染。流式服务不是把接口改成 SSE 就结束了，核心是状态如何创建、复用、取消和回收。</p>
<h2 id="基准测试怎么做"><a href="#基准测试怎么做" class="headerlink" title="基准测试怎么做"></a>基准测试怎么做</h2><p>建议至少准备三组基准：</p>
<ol>
<li>单请求基准：观察模型和预处理的最低延迟；</li>
<li>并发基准：观察队列、batch 和 p99；</li>
<li>长运行基准：观察内存、句柄、缓存和错误恢复。</li>
</ol>
<p>语音任务还要加入短音频、长音频、静音、噪声和连续 chunk。只测理想样本会低估线上复杂度。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>推理服务的关键不是“哪个框架最快”，而是哪一层正在限制系统。先把队列、预处理、prefill、decode、流式输出和取消行为拆开观测，再决定是换 serving runtime、加调度层，还是优化数据路径。</p>
<h2 id="Request-trace-schema"><a href="#Request-trace-schema" class="headerlink" title="Request trace schema"></a>Request trace schema</h2><p>Serving logs should carry a trace id across client, gateway, frontend, model runtime, and streaming output. A minimal trace separates client send time, server receive time, queue time, preprocessing, prefill, decode, postprocessing, first-token latency, final-token latency, and client receive time.</p>
<p>For speech and multimodal workloads, the trace also needs feature length, chunk count, cache reuse, cancellation state, and output flush timing. Otherwise p99 latency is visible, but not explainable.</p>
]]></content>
      <tags>
        <tag>ASR</tag>
        <tag>LLM</tag>
        <tag>Open Source</tag>
        <tag>Inference</tag>
      </tags>
  </entry>
  <entry>
    <title>语音大模型工程：音频 token、LLM 主干与对齐契约</title>
    <url>/2026/06/10/Speech-LLM-Audio-Token-Alignment/</url>
    <content><![CDATA[<p>语音大模型正在把 ASR、语音理解和多模态交互拉进 LLM 生态。Qwen3-ASR、Qwen-Omni、WeNet、CTC-AED 等路线背后都有同一个问题：音频表示如何进入语言模型，并且不破坏位置、mask、长度和训练目标的语义。</p>
<p>这类系统表面上是“接一个 speech encoder 到 LLM”，实际难点在对齐契约。张量 shape 对，不等于语义对。</p>
<span id="more"></span>

<h2 id="一条典型链路"><a href="#一条典型链路" class="headerlink" title="一条典型链路"></a>一条典型链路</h2><p>语音大模型常见链路可以拆成五段：</p>
<ol>
<li>audio frontend：采样率、分帧、特征提取或 waveform 编码；</li>
<li>speech encoder：把声学输入变成连续 hidden states 或离散 token；</li>
<li>projector：把音频侧维度映射到 LLM hidden size；</li>
<li>LLM backbone：处理文本 token、音频 token 和 prompt 模板；</li>
<li>objective heads：使用生成 loss、CTC loss、AED loss 或辅助对齐 loss。</li>
</ol>
<p>这五段每一段都可能改变长度、mask 或位置信息。工程上必须把这些变化显式记录下来，而不是只在 forward 里临时拼接。</p>
<h2 id="对齐契约有哪些"><a href="#对齐契约有哪些" class="headerlink" title="对齐契约有哪些"></a>对齐契约有哪些</h2><p><strong>长度契约</strong>：输入音频多少帧，经过 subsampling 后剩多少 token，projector 是否再改变长度，最终监督序列如何匹配。这个契约不清楚，训练 loss 下降也可能是错的。</p>
<p><strong>位置契约</strong>：文本 token 和音频 token 是否共享 RoPE，是否需要多维位置，音频 token 插入 prompt 的哪个区域，padding 是否影响位置编号。</p>
<p><strong>mask 契约</strong>：attention mask、loss mask、padding mask 不是一回事。一个位置可以参与 attention 但不参与 loss，也可以只作为上下文条件存在。把它们混在一起，是语音 LLM 最常见的隐性错误。</p>
<p><strong>目标契约</strong>：CTC 更关心单调对齐，AED 更关心序列到序列，LLM loss 更关心生成协议。多目标训练时，要能单独观察每个 loss 的行为。</p>
<h2 id="最小闭环优先于复杂结构"><a href="#最小闭环优先于复杂结构" class="headerlink" title="最小闭环优先于复杂结构"></a>最小闭环优先于复杂结构</h2><p>开源模型工程化时，不建议一开始就堆复杂结构。更稳的顺序是：</p>
<ol>
<li>固定 LLM，只训练 projector，在极小样本上验证过拟合；</li>
<li>加入明确的音频 token 长度日志；</li>
<li>检查 padding 位置不参与 loss；</li>
<li>单独跑短音频、长音频、静音片段和噪声片段；</li>
<li>再逐步解冻 encoder 或加入辅助目标。</li>
</ol>
<p>这条路径能把“系统能跑”和“系统能学”分开。前者是 smoke test，后者才是建模有效性的起点。</p>
<h2 id="Qwen-与-WeNet-的工程启发"><a href="#Qwen-与-WeNet-的工程启发" class="headerlink" title="Qwen 与 WeNet 的工程启发"></a>Qwen 与 WeNet 的工程启发</h2><p>Qwen 类模型提示我们，现代 LLM 的位置编码和输入封装并不一定是传统 HuggingFace decoder 的简单结构。不要凭字段名猜测模型内部模块，应该以官方加载入口、配置和源码为准。</p>
<p>WeNet 和 CTC-AED 路线提示我们，语音任务仍然需要单调对齐、时间边界和流式约束。LLM 主干很强，但它不能替代语音侧对齐诊断。</p>
<p>更实际的做法是把两类能力组合起来：语音侧负责稳定编码和对齐，LLM 侧负责语言建模、指令跟随和复杂生成。</p>
<h2 id="流式场景的额外约束"><a href="#流式场景的额外约束" class="headerlink" title="流式场景的额外约束"></a>流式场景的额外约束</h2><p>一旦进入流式输入，问题会更复杂：</p>
<ul>
<li>音频 chunk 之间是否共享缓存；</li>
<li>当前文本生成是否可以被新音频打断；</li>
<li>旧音频 token 是否需要压缩或丢弃；</li>
<li>取消请求时 decoder state 如何清理；</li>
<li>partial hypothesis 是否会污染最终输出。</li>
</ul>
<p>这些都不是模型结构图能解决的问题，而是运行时状态管理问题。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>语音大模型工程的核心不是把 encoder 和 LLM 拼起来，而是明确音频 token 如何获得位置、如何参与 attention、如何参与 loss、如何在流式状态中被更新。只要这些契约没有写清楚，模型越大，调试越困难。</p>
<h2 id="输入入口一致性"><a href="#输入入口一致性" class="headerlink" title="输入入口一致性"></a>输入入口一致性</h2><p>Speech-LLM debugging should include an input-contract test. The same utterance should be passed through every supported input path, then compare sampling rate, channel handling, feature length, audio-token length, position ids, attention mask, loss mask, and decoded text.</p>
<p>Path-based input often hides loading, mixdown, resampling, and normalization steps. Tensor or precomputed-feature input may bypass them. If these paths are not tested side by side, a model issue can be mistaken for an input-contract issue.</p>
]]></content>
      <tags>
        <tag>ASR</tag>
        <tag>LLM</tag>
        <tag>Open Source</tag>
        <tag>Speech</tag>
      </tags>
  </entry>
  <entry>
    <title>实时语音 Turn-taking 评测：从端点检测到可接话判断</title>
    <url>/2026/06/10/Realtime-Speech-Turn-Taking-Evaluation/</url>
    <content><![CDATA[<p>实时语音交互里，端点检测只能回答“声音是否停止”，不能直接回答“系统现在是否应该接话”。更有用的建模目标是 turn-taking：判断当前轮次是否完整、是否需要继续等待、是否允许系统响应，以及错误响应会带来什么交互代价。</p>
<p>这类问题适合公开讨论，因为它不依赖具体场景。核心是标签定义、输入噪声、延迟预算和评测切分。</p>
<span id="more"></span>

<h2 id="标签不是一个-end"><a href="#标签不是一个-end" class="headerlink" title="标签不是一个 end"></a>标签不是一个 end</h2><p>一个可用的标签体系至少要区分几类状态：</p>
<ul>
<li>continue listening：用户大概率还会继续说；</li>
<li>semantic end：语义上已经形成完整表达；</li>
<li>response-ready：系统可以开始响应；</li>
<li>hold：语义可能完整，但仍建议等待；</li>
<li>interruption risk：系统现在响应可能造成打断。</li>
</ul>
<p>如果所有状态都压成一个 end 标签，模型会把声学停顿、语义完成和可接话动作混在一起。离线 F1 可能不错，在线体验仍然不稳定。</p>
<h2 id="输入要按噪声分层"><a href="#输入要按噪声分层" class="headerlink" title="输入要按噪声分层"></a>输入要按噪声分层</h2><p>Turn-taking 评测不应该只用干净文本。建议至少准备三组输入：</p>
<ol>
<li>clean transcript，用来测语义判断上限；</li>
<li>final ASR transcript，用来测真实识别误差下的判断；</li>
<li>partial ASR transcript，用来测流式中间态。</li>
</ol>
<p>三组结果的差异可以回答一个关键问题：错误来自语义判断本身，还是来自 ASR 插入、删除、替换和 partial 不稳定。</p>
<h2 id="评测指标"><a href="#评测指标" class="headerlink" title="评测指标"></a>评测指标</h2><p>常规 precision、recall、F1 仍然有用，但不够。实时交互还需要动作级指标：</p>
<ul>
<li>false response：过早接话；</li>
<li>late response：等待过久；</li>
<li>missed response：该接话时没有接；</li>
<li>interruption recovery：误打断后能否恢复；</li>
<li>first-action latency：第一次动作延迟；</li>
<li>final-decision stability：partial 更新后决策是否频繁反复。</li>
</ul>
<p>这些指标比单一 end accuracy 更接近真实系统质量。</p>
<h2 id="一个最小评测闭环"><a href="#一个最小评测闭环" class="headerlink" title="一个最小评测闭环"></a>一个最小评测闭环</h2><p>可以从一个小闭环开始：</p>
<ol>
<li>收集短句、长句、停顿、修正、重复、半句话等样本；</li>
<li>标注 semantic end、response-ready 和 hold；</li>
<li>用 clean transcript 训练或评估上限；</li>
<li>加入 final ASR text，测识别噪声损失；</li>
<li>加入 partial text，测流式稳定性；</li>
<li>把错误分成过早、过晚、误打断、未恢复。</li>
</ol>
<p>这个闭环能把“模型是否理解语义”和“系统是否适合实时交互”分开看。</p>
<h2 id="工程注意点"><a href="#工程注意点" class="headerlink" title="工程注意点"></a>工程注意点</h2><p>线上系统需要保留 event log：partial 输入、时间戳、模型判断、动作、取消、恢复和最终输出。没有这些日志，就很难解释一次错误响应是由文本变化、模型阈值、状态机还是服务延迟造成的。</p>
<p>Turn-taking 的目标不是追求最早响应，而是在低延迟和低打断之间找到可控平衡。</p>
]]></content>
      <tags>
        <tag>ASR</tag>
        <tag>Open Source</tag>
        <tag>Evaluation</tag>
        <tag>Speech</tag>
      </tags>
  </entry>
  <entry>
    <title>PEFT 工程取舍：LoRA、Prefix 与低成本适配的边界</title>
    <url>/2026/06/10/PEFT-Engineering-Tradeoffs/</url>
    <content><![CDATA[<p>PEFT 经常被理解成“少训练一些参数”。这个理解没有错，但不够工程化。真正的选择不是参数量越少越好，而是在训练显存、推理开销、上下文占用、适配能力、部署形态和工具生态之间做权衡。</p>
<p>不同 PEFT 方法适合解决不同问题。先定义约束，再选方法，比直接比较榜单更可靠。</p>
<span id="more"></span>

<h2 id="LoRA-的稳定区间"><a href="#LoRA-的稳定区间" class="headerlink" title="LoRA 的稳定区间"></a>LoRA 的稳定区间</h2><p>LoRA 在权重矩阵旁边增加低秩更新，训练时只更新少量 adapter 参数。它的优势很明确：生态成熟、实现简单、训练稳定、容易与基础模型合并。</p>
<p>它适合做格式适配、风格适配、轻量任务迁移，也适合多 adapter 管理。问题是 LoRA 的效果依赖 rank、target modules、学习率和数据规模。rank 太小可能欠拟合，rank 太大又会接近全量微调的复杂度。</p>
<p>工程上需要记录的不只是最终指标，还包括 adapter 大小、训练吞吐、显存峰值、是否合并权重、合并后是否影响推理框架。</p>
<h2 id="Prefix-和-Prompt-Tuning-的代价"><a href="#Prefix-和-Prompt-Tuning-的代价" class="headerlink" title="Prefix 和 Prompt Tuning 的代价"></a>Prefix 和 Prompt Tuning 的代价</h2><p>Prefix-Tuning、P-Tuning 这类方法把可训练信息放进输入或注意力前缀里。它们的好处是参数少，和模型主体解耦程度高。代价是上下文长度和推理形态会变化。</p>
<p>如果任务本身需要长上下文，额外前缀会挤占窗口。如果推理框架需要频繁切换任务，前缀缓存和 batch 组织也会变复杂。</p>
<p>所以这类方法更适合明确任务边界、输入长度可控、适配目标相对稳定的场景。</p>
<h2 id="QLoRA-的门槛"><a href="#QLoRA-的门槛" class="headerlink" title="QLoRA 的门槛"></a>QLoRA 的门槛</h2><p>QLoRA 把量化和 LoRA 结合，能显著降低训练显存门槛。它的价值很大，但不要把它理解成“免费降成本”。</p>
<p>量化配置、计算 dtype、优化器状态、梯度检查点、batch size 都会影响稳定性。低显存能跑起来是一回事，收敛是否稳定、最终 adapter 是否可部署，是另一回事。</p>
<p>一套严谨实验应该同时记录：基础模型量化方式、adapter 配置、训练 dtype、显存峰值、吞吐、loss 曲线和验证集指标。</p>
<h2 id="任务类型决定上限"><a href="#任务类型决定上限" class="headerlink" title="任务类型决定上限"></a>任务类型决定上限</h2><p>PEFT 对不同任务的适配能力不同。</p>
<ul>
<li>格式学习：LoRA 往往很有效；</li>
<li>指令风格：LoRA 和部分 prompt 方法都可以尝试；</li>
<li>知识注入：PEFT 不一定可靠，需要评估是否只是记住表面模式；</li>
<li>长上下文行为：前缀类方法可能带来窗口压力；</li>
<li>多任务切换：adapter 管理和路由比单次训练更重要。</li>
</ul>
<p>如果任务需要深度改变推理能力，PEFT 可能只是降低成本的入口，不一定能替代更大规模的数据和训练策略。</p>
<h2 id="部署是选择的一部分"><a href="#部署是选择的一部分" class="headerlink" title="部署是选择的一部分"></a>部署是选择的一部分</h2><p>训练阶段方便，不代表部署阶段方便。上线前至少要回答：</p>
<ol>
<li>adapter 是否需要动态加载；</li>
<li>多 adapter 是否共享 batch；</li>
<li>是否合并到基础模型；</li>
<li>合并后权重如何版本化；</li>
<li>推理框架是否支持对应算子和 dtype；</li>
<li>回滚是否只需要切 adapter，还是要切整模型。</li>
</ol>
<p>这些问题会直接影响服务复杂度。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>PEFT 是工程组件库，不是单一算法。LoRA、Prefix、P-Tuning、QLoRA 的核心差异，不只是训练参数量，而是它们如何改变训练图、上下文窗口、推理路径和版本管理。把这些成本写清楚，实验结果才可复用。</p>
<h2 id="Adapter-registry"><a href="#Adapter-registry" class="headerlink" title="Adapter registry"></a>Adapter registry</h2><p>PEFT needs an adapter registry, not only a folder of checkpoints. Each adapter should record base model, dataset version, target modules, rank, alpha, dropout, learning rate, quantization config, evaluation set, and merge status.</p>
<p>This registry is what makes LoRA or QLoRA operationally safe. It lets teams compare adapters, roll back a bad merge, keep training data and model behavior aligned, and avoid confusing a prompt change with an adapter change.</p>
]]></content>
      <tags>
        <tag>LLM</tag>
        <tag>Open Source</tag>
        <tag>PEFT</tag>
        <tag>Fine-tuning</tag>
      </tags>
  </entry>
  <entry>
    <title>音频模型 Batch 一致性排查：从有效输入区间到逐层 diff</title>
    <url>/2026/06/10/Speech-Batch-Consistency-Debugging/</url>
    <content><![CDATA[<p>音频模型在 batch size 改变后输出不一致，是很常见但容易误判的问题。不要一开始就怀疑最后的解码结果。更稳的排查顺序是：先证明输入等价，再证明 mask 和有效长度等价，最后逐层定位数值差异从哪里开始放大。</p>
<p>这篇文章整理一套通用排查协议，适用于 ASR、VAD、speaker model 和其他时序音频模型。</p>
<span id="more"></span>

<h2 id="先定义什么叫一致"><a href="#先定义什么叫一致" class="headerlink" title="先定义什么叫一致"></a>先定义什么叫一致</h2><p>一致性可以分三层：</p>
<ul>
<li>数值一致：有效区域 hidden states 差异在阈值内；</li>
<li>决策一致：分类标签、token 或分数排序不变；</li>
<li>指标一致：WER、F1、recall 等整体指标不随 batch policy 漂移。</li>
</ul>
<p>很多线上系统真正需要的是决策一致，而不是逐位相同。先定义目标，才能避免把所有浮点差异都当成故障。</p>
<h2 id="有效输入区间优先"><a href="#有效输入区间优先" class="headerlink" title="有效输入区间优先"></a>有效输入区间优先</h2><p>排查时只比较有效输入区间，不要把 padding 区混进去。建议固定：</p>
<ol>
<li>输入样本顺序；</li>
<li>sampling rate 和 feature extraction；</li>
<li>padding value；</li>
<li>valid length；</li>
<li>attention mask 或 padding mask；</li>
<li>inference mode、dropout、random seed；</li>
<li>dtype 和 device。</li>
</ol>
<p>如果这些条件没固定，batch&#x3D;1 和 batch&gt;1 的差异没有解释价值。</p>
<h2 id="逐层-diff"><a href="#逐层-diff" class="headerlink" title="逐层 diff"></a>逐层 diff</h2><p>最小复现可以这样做：</p>
<ol>
<li>同一条样本单独推理一次；</li>
<li>把同一条样本放进混合 batch 再推理一次；</li>
<li>对齐有效长度；</li>
<li>保存每层输入输出的 max diff、mean diff、cosine similarity；</li>
<li>找到差异第一次明显放大的层。</li>
</ol>
<p>定位到层之后，再检查该层的 mask broadcast、normalization、cache、subsampling、kernel path 和 dtype 转换。</p>
<h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><p>几类问题最常见：</p>
<ul>
<li>padding 区参与了 attention 或 pooling；</li>
<li>valid length 在 subsampling 后没有同步更新；</li>
<li>mask 维度广播错误；</li>
<li>batch normalization 或统计层在推理模式下状态不一致；</li>
<li>mixed precision 在不同 batch shape 下走了不同 kernel；</li>
<li>streaming cache 没有按样本隔离。</li>
</ul>
<p>这些问题会在最终输出里表现得很混乱，但逐层 diff 通常能把范围缩小。</p>
<h2 id="回归测试"><a href="#回归测试" class="headerlink" title="回归测试"></a>回归测试</h2><p>修复后应保留 batch consistency regression：</p>
<ul>
<li>batch&#x3D;1；</li>
<li>batch&#x3D;2；</li>
<li>长短混合；</li>
<li>全短样本；</li>
<li>全长样本；</li>
<li>不同 dtype；</li>
<li>不同设备或后端。</li>
</ul>
<p>每组都只比较有效区域，并同时记录数值差异和决策差异。这样后续升级推理框架、kernel、量化策略或 batch policy 时，问题能被提前发现。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>Batch 一致性排查的关键是顺序。先证明输入和 mask 等价，再逐层找差异，不要直接从最终文本或最终分类结果反推原因。对时序模型来说，有效长度、padding、subsampling 和 dtype 往往比最后一层更早决定问题走向。</p>
]]></content>
      <tags>
        <tag>ASR</tag>
        <tag>Open Source</tag>
        <tag>Inference</tag>
        <tag>Debugging</tag>
      </tags>
  </entry>
  <entry>
    <title>Personal VAD 2.0 工程化：外部说话人嵌入与训练闭环</title>
    <url>/2026/06/09/PVAD2-engineering-loop/</url>
    <content><![CDATA[<p>普通 VAD 只判断“有没有人声”，Personal VAD 还要判断“是不是目标说话人”。在端侧语音识别里，这个区别很关键：系统不仅要过滤静音和噪声，还要在多人说话场景下保留目标说话人的语音片段。</p>
<p>这篇文章记录一次把 Personal VAD 2.0 论文思路落到可训练工程骨架时的设计取舍。重点是模型接口、外部 speaker embedding、数据 manifest、训练闭环和标签对齐风险。</p>
<p>项目代码：<a href="https://github.com/fclearner/Personal-vad-2.0">https://github.com/fclearner/Personal-vad-2.0</a>。</p>
<span id="more"></span>

<h2 id="任务定义"><a href="#任务定义" class="headerlink" title="任务定义"></a>任务定义</h2><p>Personal VAD 可以建模为逐帧三分类任务：</p>
<ul>
<li><code>0</code>：目标说话人语音；</li>
<li><code>1</code>：非目标说话人语音；</li>
<li><code>2</code>：非语音。</li>
</ul>
<p>和普通 VAD 相比，PVAD 多了一个目标说话人的条件输入。这个条件通常来自 enrollment 音频，经过 speaker encoder 提取成 embedding，再输入 PVAD 主模型。</p>
<p>一个可落地的工程接口可以保持很窄：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">logits = model(features, embedding)</span><br><span class="line">logits, lengths = model(features, embedding, lengths, return_lengths=<span class="literal">True</span>)</span><br></pre></td></tr></table></figure>

<p>其中：</p>
<ul>
<li><code>features</code>：<code>(batch, frames, 512)</code>；</li>
<li><code>embedding</code>：<code>(batch, speaker_embedding_dim)</code> 或 <code>(batch, frames, speaker_embedding_dim)</code>；</li>
<li><code>logits</code>：<code>(batch, frames_out, 3)</code>。</li>
</ul>
<p><code>embedding=None</code> 时可以用零向量表示无 enrollment 条件，这样同一套模型能覆盖“有目标说话人条件”和“无条件 VAD 退化模式”。</p>
<h2 id="模型结构"><a href="#模型结构" class="headerlink" title="模型结构"></a>模型结构</h2><p>一个实用的 PVAD 骨架可以拆成四层：</p>
<ol>
<li>acoustic encoder：处理堆叠后的声学特征；</li>
<li>speaker projection：把外部 speaker embedding 投影到 encoder 维度；</li>
<li>speaker conditioning：用 speaker 信息调制 acoustic 表示；</li>
<li>frame classifier：输出三类逐帧 logits。</li>
</ol>
<p>工程上最容易出问题的地方不是分类头，而是 speaker 条件如何进入主干网络。</p>
<p>一种稳妥做法是让 speaker embedding 先经过投影层，再生成调制参数：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">speaker embedding</span><br><span class="line">  -&gt; speaker_embedding_proj</span><br><span class="line">  -&gt; speaker_pre_net</span><br><span class="line">  -&gt; FiLM / cosine conditioning</span><br><span class="line">  -&gt; acoustic encoder states</span><br></pre></td></tr></table></figure>

<p>这样做有两个好处：</p>
<ul>
<li>embedding 维度不需要写死，64 维、192 维或其他外部向量都可以通过投影层接入；</li>
<li>speaker encoder 可以先冻结在 PVAD 训练图之外，降低第一阶段闭环难度。</li>
</ul>
<p>如果一开始就把 speaker extractor、PVAD encoder 和训练目标端到端绑在一起，数据、显存、收敛和调试成本都会同时上升。更稳的顺序是先把外部 embedding 文件接进训练，再决定是否做联合训练或蒸馏。</p>
<h2 id="外部-Speaker-Embedding"><a href="#外部-Speaker-Embedding" class="headerlink" title="外部 Speaker Embedding"></a>外部 Speaker Embedding</h2><p>外部 speaker embedding 的推荐接入方式是预提取，而不是在线抽取：</p>
<ol>
<li>enrollment 音频进入 speaker verification 模型；</li>
<li>导出固定维度 embedding，例如 CAM++ 常见的 192 维向量；</li>
<li>将 embedding 保存为 <code>.npy</code> 或 <code>.pt</code>；</li>
<li>训练 manifest 中引用对应 embedding 文件；</li>
<li>PVAD 模型通过 <code>speaker_embedding_dim</code> 读取并投影。</li>
</ol>
<p>例如：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">model = Pvad2(speaker_embedding_dim=<span class="number">192</span>)</span><br></pre></td></tr></table></figure>

<p>这条路径把 speaker encoder 和 PVAD 主训练解耦。第一阶段只需要验证三件事：</p>
<ul>
<li>embedding shape 是否稳定；</li>
<li>embedding 是否能和样本 speaker identity 对齐；</li>
<li>PVAD forward、loss、checkpoint 是否能稳定跑通。</li>
</ul>
<p>等真实数据闭环完成后，再比较不同 speaker 条件策略：</p>
<ul>
<li>零 embedding；</li>
<li>随机或已有 embedding；</li>
<li>CAM++ &#x2F; 3D-Speaker embedding；</li>
<li>外部 embedding 加轻量 adapter；</li>
<li>speaker encoder 蒸馏或联合训练。</li>
</ul>
<h2 id="数据-Manifest"><a href="#数据-Manifest" class="headerlink" title="数据 Manifest"></a>数据 Manifest</h2><p>训练数据最适合用 manifest 显式描述，而不是把路径规则写死在 dataset 里。JSONL 可以覆盖大多数场景：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;utt001&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;features&quot;</span><span class="punctuation">:</span> <span class="string">&quot;features/utt001.npy&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;labels&quot;</span><span class="punctuation">:</span> <span class="string">&quot;labels/utt001.npy&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;embedding&quot;</span><span class="punctuation">:</span> <span class="string">&quot;embeddings/spk001.npy&quot;</span><span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;utt002&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;features&quot;</span><span class="punctuation">:</span> <span class="string">&quot;features/utt002.npy&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;labels&quot;</span><span class="punctuation">:</span> <span class="string">&quot;labels/utt002.npy&quot;</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>字段约束：</p>
<ul>
<li><code>features</code>：<code>(frames, 512)</code>；</li>
<li><code>labels</code>：<code>(frames,)</code>；</li>
<li><code>embedding</code>：可选，<code>(speaker_embedding_dim,)</code>；</li>
<li>未提供 <code>embedding</code> 时，用零向量补齐。</li>
</ul>
<p>路径相对 manifest 文件所在目录解析，这样数据目录迁移后不需要重写所有样本路径。</p>
<p>对 frame-level 任务，manifest 里最重要的不是路径本身，而是 feature frame 和 label frame 是否严格对齐。只要这里错一帧，训练 loss 仍然可能下降，但指标会失真。</p>
<h2 id="训练闭环"><a href="#训练闭环" class="headerlink" title="训练闭环"></a>训练闭环</h2><p>最小训练闭环建议包含：</p>
<ul>
<li>manifest dataset；</li>
<li>padding-aware collate；</li>
<li><code>CrossEntropyLoss(ignore_index=-100)</code>；</li>
<li>train &#x2F; valid loop；</li>
<li><code>last.pt</code> 和 <code>best.pt</code> checkpoint；</li>
<li>CPU synthetic smoke test；</li>
<li>小样本 overfit test。</li>
</ul>
<p>训练入口可以保持直接：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python train.py \</span><br><span class="line">  --train-manifest data/small_train.jsonl \</span><br><span class="line">  --valid-manifest data/small_valid.jsonl \</span><br><span class="line">  --speaker-embedding-dim 192 \</span><br><span class="line">  --epochs 50 \</span><br><span class="line">  --batch-size 4 \</span><br><span class="line">  --device cuda</span><br></pre></td></tr></table></figure>

<p>在没有真实数据前，synthetic smoke test 只能证明代码路径能跑，不能证明模型有效。真正有价值的第一个实验应该是小样本 overfit：</p>
<ul>
<li>loss 能稳定下降；</li>
<li>frame accuracy 明显升高；</li>
<li>padding 位置不参与 loss；</li>
<li>logits 不出现 NaN；</li>
<li>checkpoint 能保存和恢复；</li>
<li>enrollment 缺失样本不会破坏 batch。</li>
</ul>
<h2 id="Enrollment-less-Augmentation"><a href="#Enrollment-less-Augmentation" class="headerlink" title="Enrollment-less Augmentation"></a>Enrollment-less Augmentation</h2><p>Personal VAD 2.0 里一个重要训练机制是 enrollment-less augmentation。直觉上，它让模型在没有目标说话人条件时退化成更接近普通 VAD 的行为。</p>
<p>一个可实现的版本是：</p>
<ol>
<li>对部分 utterance 采样 dropout mask；</li>
<li>将这些样本的 speaker embedding 置零；</li>
<li>将这些样本里的非目标说话人语音标签 <code>1</code> 改成目标语音标签 <code>0</code>；</li>
<li>padding label <code>-100</code> 保持不变。</li>
</ol>
<p>这一步必须同时改 embedding 和 label。如果只把 embedding 置零，不改 label，模型会在“无目标说话人信息”的条件下被要求区分目标与非目标，说法上就不一致。</p>
<h2 id="Subsampling-与标签对齐"><a href="#Subsampling-与标签对齐" class="headerlink" title="Subsampling 与标签对齐"></a>Subsampling 与标签对齐</h2><p>PVAD 很容易在 subsampling 上踩坑。假设模型支持两种入口：</p>
<ul>
<li><code>linear</code>：特征已经按预期堆叠或下采样后送入模型；</li>
<li><code>conv2d3</code>：模型内部做卷积子采样。</li>
</ul>
<p>如果 logits 长度和 labels 长度不同，简单截断或补 ignore index 只能让代码继续跑，不等于语义正确。更严谨的做法是为每一种 subsampling 明确定义 label 下采样策略，例如：</p>
<ul>
<li>取中心帧标签；</li>
<li>窗口多数投票；</li>
<li>目标语音优先；</li>
<li>非语音优先；</li>
<li>混合帧标记为 ignore。</li>
</ul>
<p>策略选择会直接影响 precision、recall 和误报行为，不能隐藏在 collate 或 loss 的容错逻辑里。</p>
<h2 id="实验路线"><a href="#实验路线" class="headerlink" title="实验路线"></a>实验路线</h2><p>比起继续堆模型结构，更值得优先跑通的是数据和评价闭环：</p>
<ol>
<li>准备 5 到 20 条真实样本，覆盖目标说话人、非目标说话人、非语音、长短句和 enrollment 缺失；</li>
<li>跑小样本 overfit，确认 loss、accuracy、padding 和 checkpoint；</li>
<li>固定 PVAD 主干，比较零 embedding、外部 embedding 和轻量 adapter；</li>
<li>加入真实验证集，拆分 target speech recall、non-target false accept、non-speech false alarm；</li>
<li>再考虑 streaming cache、ONNX、量化和端侧延迟。</li>
</ol>
<p>工程判断上，第一阶段的目标不是复现论文最终指标，而是证明这个系统具备可训练、可评估、可替换 speaker embedding 的闭环。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>PVAD 的难点不只是模型结构，而是 speaker 条件、frame label、训练目标和数据组织必须同时对齐。一个合理的工程落点是：</p>
<ul>
<li>speaker embedding 维度配置化；</li>
<li>外部 embedding 先离线接入；</li>
<li>manifest 显式记录 feature、label 和 embedding；</li>
<li>enrollment-less augmentation 成对修改 embedding 与 label；</li>
<li>subsampling 必须配套定义 label 对齐；</li>
<li>smoke test 只验证可运行，小样本 overfit 才开始验证可学习。</li>
</ul>
<p>把这些边界先定清楚，后续再替换 speaker encoder、加入 adapter 或做端侧优化，才不会把模型调试和数据问题混在一起。</p>
<h2 id="Speaker-条件消融"><a href="#Speaker-条件消融" class="headerlink" title="Speaker 条件消融"></a>Speaker 条件消融</h2><p>PVAD should include speaker-conditioning ablations before architecture changes. Compare at least three modes: no speaker condition, random or mismatched speaker condition, and matched speaker condition.</p>
<p>The expected result is not only higher frame-level F1. The matched condition should improve target-speaker recall, the mismatched condition should degrade predictably, and non-speech false alarms should remain controlled. This proves the model is using the speaker condition rather than learning a generic VAD shortcut.</p>
]]></content>
      <tags>
        <tag>ASR</tag>
        <tag>VAD</tag>
        <tag>PVAD</tag>
        <tag>Speaker Embedding</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo&amp;Github construct blog</title>
    <url>/2019/07/09/hexo-github-constructBlog/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>以前一直想自己写个博客网站，无奈一直没有时间，最近在师兄和好兄弟的推荐下，使用了hexo这一基于Node.js的静态博客框架，并将其托管于github上。总算是圆了个梦！</p>
<span id="more"></span>
<h2 id="搭建步骤"><a href="#搭建步骤" class="headerlink" title="搭建步骤"></a>搭建步骤</h2><ul>
<li>github创建个人仓库</li>
<li>安装git</li>
<li>安装Node.js</li>
<li>安装hexo</li>
<li>上传博客内容至github仓库</li>
<li>绑定域名</li>
<li>其它</li>
<li>附录</li>
</ul>
<h3 id="github搭建个人仓库"><a href="#github搭建个人仓库" class="headerlink" title="github搭建个人仓库"></a>github搭建个人仓库</h3><p>登录github,在github仓库中创建新仓库, 仓库名按:<strong>用户名.github.io</strong>的格式命名, 注意:这个用户名使用你的GitHub帐号名称代替，否则在博客创建时会出现无法工作的情况。</p>
<h3 id="安装git"><a href="#安装git" class="headerlink" title="安装git"></a>安装git</h3><p>git是一个分散式版本控制软件，最初由林纳斯·托瓦兹创作，于2005年以GPL释出。最初目的是为更好地管理Linux核心开发而设计。请自行搜索git下载工具。<br>下载安装完成后，windows右键中即可出现git bash字样。<br><img src="/2019/07/09/hexo-github-constructBlog/gitbash.png"><br>以下步骤是为了省去每次上传github都需要繁复输入密码:</p>
<h4 id="设置用户名和邮箱"><a href="#设置用户名和邮箱" class="headerlink" title="设置用户名和邮箱"></a>设置用户名和邮箱</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git config --global user.name &quot;你的GitHub用户名&quot;</span><br><span class="line">git config --global user.email &quot;你的GitHub注册邮箱&quot;</span><br></pre></td></tr></table></figure>
<h4 id="生成ssh密钥文件"><a href="#生成ssh密钥文件" class="headerlink" title="生成ssh密钥文件"></a>生成ssh密钥文件</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa -C &quot;你的GitHub注册邮箱&quot;</span><br></pre></td></tr></table></figure>
<h4 id="复制密钥"><a href="#复制密钥" class="headerlink" title="复制密钥"></a>复制密钥</h4><p>找到生成的.ssh的文件夹中的id_rsa.pub密钥, 将内容全部复制<br><img src="/2019/07/09/hexo-github-constructBlog/ssh.jpg"></p>
<h4 id="新建SSH-key"><a href="#新建SSH-key" class="headerlink" title="新建SSH key"></a>新建SSH key</h4><p>打开GitHub_Settings_keys 页面, 新建new SSH Key, 将复制的内容写入key部分, title的名字可随意命名, 最后点击 Add SSH Key。<br><img src="/2019/07/09/hexo-github-constructBlog/newSSHKey.jpg"><br>添加成功后可按下图测试。<br><img src="/2019/07/09/hexo-github-constructBlog/SSHTest.jpg"></p>
<h3 id="安装Node-js"><a href="#安装Node-js" class="headerlink" title="安装Node.js"></a>安装Node.js</h3><p>请自行搜索Node.js进行安装, 安装完成后, 可按下图测试。<br><img src="/2019/07/09/hexo-github-constructBlog/NodeJsTest.jpg"></p>
<h3 id="安装hexo"><a href="#安装hexo" class="headerlink" title="安装hexo"></a>安装hexo</h3><p>在命令行中输入</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install -g hexo-cli</span><br></pre></td></tr></table></figure>
<p>安装时间较长, 安装完成后, 初始化博客, 输入</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo init blog</span><br></pre></td></tr></table></figure>
<p>下列为hexo常用命令:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo new myblogpage</span><br><span class="line">hexo g</span><br><span class="line">hexo s</span><br><span class="line">hexo d</span><br><span class="line">hexo clean</span><br></pre></td></tr></table></figure>
<p>hexo new 可生成新的页面<br>hexo g 生成博客<br>hexo s 启动服务器预览<br>hexo d 部署至github仓库<br>hexo clean 清除缓存</p>
<h3 id="上传博客内容至github仓库"><a href="#上传博客内容至github仓库" class="headerlink" title="上传博客内容至github仓库"></a>上传博客内容至github仓库</h3><p>首先将之前创建的github仓库与我们的博客相关联, 打开下图的_config.yml文件,该文件为hexo blog的设置文件,可在内部修改一些基础设置。<br><img src="/2019/07/09/hexo-github-constructBlog/BlogConfig1.jpg"><br>在文件的最后, 按下图编辑修改, 注意:由于语法格式问题, 冒号后请空一格, 否则无法正常工作。<br><img src="/2019/07/09/hexo-github-constructBlog/BlogConfig2.jpg"><br>设置完成后, 安装git部署插件, 输入</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>
<p>接着, 我们分别输入三条命令</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo clean </span><br><span class="line">hexo g </span><br><span class="line">hexo d</span><br></pre></td></tr></table></figure>
<p>接着访问<strong>XXX.github.io</strong>, 你会发现你的博客已经上线。</p>
<h3 id="绑定域名"><a href="#绑定域名" class="headerlink" title="绑定域名"></a>绑定域名</h3><p>上述步骤实现后, 博客已经可以正常上线, 接下来只需学会markdown的基础写作(超简单)即可正式发布博客。<br>但是若你不满足于XXX.github.io的网址形式, 想要个性化。那么即可购买域名与XXX.github.io进行绑定。以阿里云域名构建为例：<br>购买域名前,请确认你想构建的域名是否已存在。再进行购买。<br><img src="/2019/07/09/hexo-github-constructBlog/domain1.jpg"><br>域名购买成功后,按下图进入解析界面:<br><img src="/2019/07/09/hexo-github-constructBlog/domain2.jpg"><br>添加如下两个记录:<br><img src="/2019/07/09/hexo-github-constructBlog/domain3.jpg"><br>接着在github的仓库设置界面填入你的域名,保存<br><img src="/2019/07/09/hexo-github-constructBlog/domain4.jpg"><br>在本地blog&#x2F;source文件夹下, 新建CNAME记事本文件, 输入域名, 建议不要加www, 保存成<strong>所有文件</strong>而不是<strong>txt</strong>文件。<br>接着在blog目录下执行以下三条命令即可:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo g</span><br><span class="line">hexo d</span><br></pre></td></tr></table></figure>
<p>域名绑定成功！</p>
<h3 id="其它"><a href="#其它" class="headerlink" title="其它"></a>其它</h3><p>如果你还想更换博客样式的话, 请查阅hexo更换主题相关内容, 常用的主题为next。<br>hexo图片加载缓慢时,可采用图床加快加载速度,具体请搜索hexo图床相关内容。</p>
<p>[错误 Usage: hexo &lt; command &gt; 解决]<br>这是太长时间没使用hexo的缘故，hexo更新使得原来的本地服务不能使用，需要重新部署一下hexo本地服务器<br>在hexo的根目录下输入命令：npm install hexo-server –save<br>如果没有在hexo的根目录下输入该命令，则会报错找不到package.json文件</p>
<h2 id="附录"><a href="#附录" class="headerlink" title="附录"></a>附录</h2><p>参考: <a href="https://zhuanlan.zhihu.com/p/26625249">https://zhuanlan.zhihu.com/p/26625249</a></p>
]]></content>
      <tags>
        <tag>Blog</tag>
        <tag>Github</tag>
      </tags>
  </entry>
  <entry>
    <title>语音对话合成数据工程：Schema、口语化与质量闸门</title>
    <url>/2026/06/11/Speech-Dialog-Data-Synthesis-Quality-Gates/</url>
    <content><![CDATA[<p>用大模型合成对话数据很容易，难的是让这批数据真正进入语音系统的训练和评测闭环。一个可用的语音对话样本，不只是几轮看起来顺畅的文本。它还要保留角色、话轮、时间、通道、实体槽位、ASR 噪声、口语现象和质量标记。</p>
<p>如果这些约束只写在 prompt 里，数据规模一大就会漂移：格式不稳定、标签不一致、口语化过度、实体边界模糊，最后训练出来的模型学到的不是交互能力，而是一批生成器的随机习惯。</p>
<span id="more"></span>

<h2 id="先定义数据契约"><a href="#先定义数据契约" class="headerlink" title="先定义数据契约"></a>先定义数据契约</h2><p>合成数据的第一层不是 prompt，而是 schema。每条样本至少要回答几个问题：</p>
<ul>
<li>这句话属于哪个对话、哪个话轮、哪个角色；</li>
<li>是否来自单通道还是多通道音频；</li>
<li>是否有时间边界；</li>
<li>文本是干净转写、ASR 候选、纠错结果还是标注结果；</li>
<li>是否包含实体、意图、结束点或其他结构化标签；</li>
<li>是否经过噪声注入、口语改写或人工复核。</li>
</ul>
<p>一个通用的 JSONL 样本可以长这样：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;key&quot;</span><span class="punctuation">:</span> <span class="string">&quot;dialog-001-turn-003&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;dialog_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;dialog-001&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;turn_id&quot;</span><span class="punctuation">:</span> <span class="number">3</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;role&quot;</span><span class="punctuation">:</span> <span class="string">&quot;user&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;channel&quot;</span><span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;start_ms&quot;</span><span class="punctuation">:</span> <span class="number">4280</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;end_ms&quot;</span><span class="punctuation">:</span> <span class="number">6910</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;txt&quot;</span><span class="punctuation">:</span> <span class="string">&quot;我想查一下刚才那个信息&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;slots&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;history&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span><span class="attr">&quot;role&quot;</span><span class="punctuation">:</span> <span class="string">&quot;assistant&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;txt&quot;</span><span class="punctuation">:</span> <span class="string">&quot;您好，请问需要查询什么内容&quot;</span><span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;quality_flags&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;spoken_style&quot;</span><span class="punctuation">,</span> <span class="string">&quot;schema_valid&quot;</span><span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>字段名可以按项目调整，但契约要稳定。训练、评测、过滤、回溯都应该围绕这套契约运行，而不是每个脚本临时解释一版格式。</p>
<h2 id="口语化必须受控"><a href="#口语化必须受控" class="headerlink" title="口语化必须受控"></a>口语化必须受控</h2><p>真实语音对话里有停顿、重复、修正、附和、打断、无效输入和噪声。但这些现象不能随机撒进文本里。</p>
<p>口语化最好拆成可控变量：</p>
<ul>
<li>filler：语气词和停顿词；</li>
<li>repetition：自然重复和异常复读；</li>
<li>repair：说错后自我修正；</li>
<li>interruption：用户打断或系统抢话；</li>
<li>invalid input：沉默、超时、无意义片段；</li>
<li>background event：背景人声、环境噪声、外放音。</li>
</ul>
<p>每个变量都应该有触发条件、比例、最大强度和质量标记。比如重复可以提升真实感，但如果不区分“自然确认”和“异常复读”，文本纠错或语义结束点模型会被带偏。口语化不是越多越好，它是数据分布的一部分。</p>
<h2 id="N-best-不是随便造错字"><a href="#N-best-不是随便造错字" class="headerlink" title="N-best 不是随便造错字"></a>N-best 不是随便造错字</h2><p>ASR n-best 数据很适合训练文本纠错、重排序和上下文恢复模型。但 n-best 的候选不能只是随机替换字符。</p>
<p>更稳的做法是把 ASR 错误拆成几类：</p>
<ul>
<li>substitution：同音、近音或领域词混淆；</li>
<li>deletion：短词、语气词或弱读成分缺失；</li>
<li>insertion：重复词、填充词或误触发热词；</li>
<li>segmentation drift：切分边界前后错位；</li>
<li>normalization drift：数字、英文、缩写和口语读法不一致。</li>
</ul>
<p>每个候选都应该保留错误类型和生成原因。这样后续模型评估时，才能知道它是在纠正同音错误，还是只是在把所有口语文本改得更书面。</p>
<h2 id="历史上下文要筛选"><a href="#历史上下文要筛选" class="headerlink" title="历史上下文要筛选"></a>历史上下文要筛选</h2><p>对话模型经常需要 history，但把完整历史都塞进去会带来两个问题：上下文预算浪费，以及弱相关历史污染当前判断。</p>
<p>可以把 history 处理成三层：</p>
<ol>
<li>固定窗口：保留最近若干轮；</li>
<li>相似度过滤：只保留与当前文本相关的历史片段；</li>
<li>结构化摘要：保留已确认实体、未完成意图和关键约束。</li>
</ol>
<p>这里的重点不是“history 越多越好”，而是当前样本需要什么信息。对于 ASR 纠错，历史可以帮助恢复实体和上下文；对于语义结束点判断，过长历史可能反而让模型忽视当前话轮是否已经完整。</p>
<h2 id="质量闸门要前置"><a href="#质量闸门要前置" class="headerlink" title="质量闸门要前置"></a>质量闸门要前置</h2><p>合成数据进入训练前，至少需要四类检查。</p>
<p>第一是 schema 检查。字段是否齐全，枚举值是否在白名单内，时间边界是否单调，角色和通道是否一致。</p>
<p>第二是文本检查。空文本、异常重复、控制 token 泄漏、过长样本、标点风格混乱、数字规范不一致，都应该在这一层处理。</p>
<p>第三是标签检查。实体 span 是否能在文本中找到，结束点标签是否互斥，意图标签是否与角色匹配，噪声标签是否和样本内容一致。</p>
<p>第四是分布检查。口语化比例、错误类型比例、场景覆盖、长度分布、角色占比、无效输入占比，都要按批次输出报告。</p>
<p>如果只抽几条样本肉眼看，合成数据很容易显得“质量不错”。真正的问题通常出现在分布层：某类标签被生成器过度使用，某类错误完全缺失，或者格式通过率随批次下降。</p>
<h2 id="训练集和评测集要分离设计"><a href="#训练集和评测集要分离设计" class="headerlink" title="训练集和评测集要分离设计"></a>训练集和评测集要分离设计</h2><p>合成数据可以扩充训练集，但评测集不能照搬同一个生成策略。否则模型可能只是在适应生成器风格。</p>
<p>评测集应该额外保留：</p>
<ul>
<li>人工或高置信 gold subset；</li>
<li>未参与 prompt 示例的模板和表达；</li>
<li>更真实的 ASR 错误分布；</li>
<li>边界样本和负样本；</li>
<li>按错误类型拆开的指标。</li>
</ul>
<p>端到端准确率只是最终结果。更有用的是知道模型在哪类样本上失败：是实体召回不够、历史过滤错误、n-best 候选没有正确答案，还是口语化扰动超出了训练分布。</p>
<h2 id="可复用的流水线"><a href="#可复用的流水线" class="headerlink" title="可复用的流水线"></a>可复用的流水线</h2><p>一个可维护的合成数据流水线可以拆成七步：</p>
<ol>
<li>定义 schema、标签枚举和质量标记；</li>
<li>生成干净对话骨架；</li>
<li>注入口语现象和交互事件；</li>
<li>生成 ASR-like n-best 或噪声文本；</li>
<li>做 schema、文本和标签校验；</li>
<li>输出批次质量报告；</li>
<li>抽样复核并回写 reason code。</li>
</ol>
<p>这里最重要的是最后一步。不要只保留“通过&#x2F;不通过”，而要保留原因：格式错、标签冲突、重复异常、历史不相关、实体不可定位、噪声过强。reason code 会成为下一轮 prompt、规则和模型改进的依据。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>合成语音对话数据的核心不是让大模型多写几轮对话，而是把生成过程变成可约束、可审计、可复现的数据工程。</p>
<p>schema 决定数据能不能被下游稳定消费；受控口语化决定数据是否接近真实语音；ASR-like 噪声决定纠错和重排序任务是否有训练价值；质量闸门决定扩量是否会变成污染。</p>
<p>真正值得沉淀的不是某个 prompt，而是一套能持续回答这些问题的流水线：这条数据为什么生成、改动了什么、保留了什么标签、通过了哪些检查、失败时应该归因到哪里。</p>
]]></content>
      <tags>
        <tag>ASR</tag>
        <tag>Open Source</tag>
        <tag>Evaluation</tag>
        <tag>Data</tag>
        <tag>Speech</tag>
      </tags>
  </entry>
</search>
